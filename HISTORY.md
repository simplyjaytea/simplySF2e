# simplyPF2e — session history

Full session-by-session narrative, process notes, and the bug log. Not loaded by default context the way CLAUDE.md is — read this when you need to know *why* something is the way it is, whether a past session already investigated something, or what a specific PR actually changed. Newest first.

## 2026-09-05 — Authorized audit publication

- The user explicitly requested pushing and merging the independently reviewed audit commit `1ee5b5b`. Publish `codex/forge-generator-audit` through a PR after CI, then verify the automatic release. Runtime code is unchanged since the final review. Revised-code installation/native Foundry acceptance remains outstanding.

## 2026-09-05 — Item Forge and complete generator audit

- Started clean from `b043a51` (PR #103, published **v0.3.5.63**) on `codex/forge-generator-audit`. Audited forge mechanics/UI, NPC/monster/encounter generation, supported PCs, provider/security boundaries, and existing tests in parallel. Full findings and evidence: [docs/audit-2026-09-05.md](docs/audit-2026-09-05.md).
- Live v0.3.5.63 on Foundry **14.365 / PF2e 8.5.0** reproduced clipped kind hints and duplicate NPC Perception. Directory placement, exclusive kind switching/preserved controls, Ghost Touch weapon and Slick armor preview/native price-level parity, and mundane NPC one-click creation with full HP/no spells passed. Added and labeled exactly two world items and one NPC; preserved prior QA artifacts. Provider usage **32,172 tokens**. These runs test the installed baseline, not the revised branch.
- Fixed forge tile heights, generation reentry/final cancellation, and item commit-before-presentation. Forge AI now chooses enum/scale values; passives/self-buffs clone unchanged eligible published equipment rules, and activations use existing module-owned benchmarks. Required base/potency choices fail closed. Activation charges must persist before effects; same-client clicks are guarded, usable copies are selected, shared macros survive while referenced, and owner-triggered night rests recharge.
- Fixed exact/atomic loot rerolls, final NPC/encounter/PC cancellation checks, original GM-brief propagation and prompt precedence, intentional empty-loot preservation, ammunition allowlists, unit quantities and pack pricing, missing-source pre-write failure, small-party encounter budgets and Low-threat adjustment, and duplicate Perception skill entries. PC loadout no longer mistakes ordinary worn gear for armor. PC feat slots use native class schedules/earned-level prerequisites; Investigator restricted skill slots use conservative mental-skill/Lore proof.
- Fresh independent reviewer `independent_audit_review` inspected the full diff and actual PF2e sources. Review caught and resolved artifact/prerequisite source leakage, offered-but-unsupported targets, an uninvested copy shadowing a usable one, and late PC cancellation permitting a write. Final verdict: **no remaining actionable findings**. It independently ran all **82 regression files** and relevant syntax/whitespace checks. A real 387-document equipment sample retained ordinary usable options after restrictions; it is not full-pack balance certification.
- Final parent gates on **Node 22.23.2** (CI major) passed **82 regression files**, **117 script syntax checks**, both JSON files, and `git diff --check`. Actual forge template/CSS with native Foundry CSS passed copy-containment/no-horizontal-overflow checks at **360/480/720px**. Revised code is committed locally, unpublished and not installed. No authenticated GitHub write or release performed.
- Limits retained: existing companion commands/mechanics are not automatically migrated; concurrent cross-client activations are not atomic; source screening is conservative, not complete prose eligibility or official custom-item balance; Investigator methodology-only/generic restricted choices remain excluded. Revised-branch native QA awaits authorized publication/test installation.

## 2026-09-05 — Authorized cleanup publication and branch maintenance

- The user requested pushing and merging the reviewed repository cleanup, followed by branch cleanup. Proceed through PR checks and the automatic release; do not push main directly. Local cleanup removes only merged references and clean, unused merged worktrees, preserving unique commits and user data. This request does not perform a Foundry update or consume provider tokens. Final deletion inventory is local git metadata (`.git/repo-cleanup-report.json`), avoiding a second release solely to record housekeeping.

## 2026-09-05 — Repository cleanup and state reconciliation

- Started from a stale local `main` at `62fb942`, fetched the remote, and created `codex/repo-cleanup` from `7f8558e`. Confirmed PR #102 is merged and release **v0.3.5.62** is published with `module.json` and `module.zip`; earlier handoff instructions to publish that feature are superseded. Its module-only update/live UI QA remains unrecorded. Last recorded installed version is v0.3.5.60.
- Traced shared HTML escaping and reproduced two failures: absent Foundry globals threw, while missing `escapeHTML` returned raw markup. `text.mjs` now escapes all five HTML-sensitive characters without a Foundry dependency. Independent review identified the quoted-attribute boundary in preset dialogs, so the final helper guarantees quote escaping itself instead of relying on an unverified native-helper contract. Preset dialogs import this owner instead of maintaining a separate Handlebars wrapper. The new production-helper regression covers independence from Foundry globals/helpers, hostile text, nulls, and paragraph wrapping; it failed before the fix and passed afterward.
- Removed unused `.spf-busy` and `.spf-itemforge-button i` styles after checking every template/app caller. Current shared progress and directory-button styles already own those paths. Added narrowly scoped ignore rules for user-owned `.claude/`, OS metadata, and release artifacts without modifying their contents.
- Replaced accumulated current-state narratives with a concise capability/release snapshot and live baton. Corrected obsolete fuzzy-resolution, runed pricing, and PC spell-slot documentation; added dependency-free local verification commands to README. Historical release/QA entries remain intact. Existing live artifacts and unresolved acceptance are preserved in HANDOFF.md.
- Baseline 68-regression run passed. Final checks passed all **69 regression files**, **104 script syntax checks**, module/localization JSON parsing, and whitespace checks on local Node **26.7.0** (CI targets Node 22). Independent reviewer `cleanup_final_review` approved with no correctness findings, including direct preset-dialog integration probes. Removed the obsolete macro-test escape stub at the reviewer's suggestion; its syntax and regression checks passed again. No GitHub write, provider spend, module update, or world mutation occurred.

## 2026-09-05 — Item Forge directory and kind-selector refresh

- Started `feat/item-forge-ui` from the local post-v0.3.5.61 documentation. The Items-directory forge entry now inserts a dedicated full-width row directly after the directory header and before item-list content, below native Create Item/Create Folder controls. Actor-directory placement, access checks, duplicate guards, and singleton opening are unchanged.
- Replaced per-render radio listeners with ApplicationV2 `data-action` choice tiles. Wondrous, Weapon, and Armor expose exclusive selected/`aria-pressed` state from private input, retain prompt/level/rarity through rerenders, include localized hints and icons, reject invalid kinds, and use intrinsic auto-fit sizing so narrow resizable windows do not depend on viewport media queries.
- Initial independent review found the first pass still used viewport responsiveness, omitted icon context, had an underpowered directory fixture, referenced unavailable CSS variables, and duplicated a localization key. Parent corrected all findings and added a production-class action regression plus exact `[header, forge row, directory list]` order coverage. All 68 regressions, 103 script syntax checks, JSON parsing, and whitespace gates pass; follow-up reviewer `6200e335-ec68-4bc7-9e66-5a613aa140d3` approved the final diff with no findings.
- No provider request, Item creation, world mutation, GitHub write, module update, or live acceptance occurred before the final gate. User explicitly requested GitHub CLI publication and module-only Foundry update/live UI QA afterward.

## 2026-09-05 — GitHub Actions Node 24 runtime maintenance

- Continued from the committed post-v0.3.5.60 QA branch onto `chore/github-actions-node24`. Updated every `actions/checkout` and `actions/setup-node` reference across PR checks, auto-release, and the reusable release workflow from v4 to v5, addressing the GitHub runner's deprecated Node 20 action-runtime annotation. The explicit project runtime remains Node 22.
- The eight version-string substitutions leave permissions, checkout depth, triggers, verification commands, release versioning, and reusable-workflow behavior unchanged. Local gates passed all 67 regression files, 102 script syntax checks, Node-based parsing of `module.json` and `lang/en.json`, and whitespace checks. `jq`, `actionlint`, Ruby, and PyYAML were unavailable, so workflow syntax/runtime still requires CI confirmation. Independent reviewer `640d1683-e045-4f31-96d5-2e3f4322463f` approved with no findings after checking complete action coverage, Node/runtime separation, hosted-runner compatibility, fetch depth, permissions, and reusable release wiring; its validation was static.
- After explicit user authorization, pushed commit `f9b59d0`, opened PR #101, and verified successful Pull Request Checks run `33944344962`. PR #101 merged as `e03c4a1`; Auto Release run `33944387562` exercised the upgraded actions successfully and published **v0.3.5.61** with `module.json` and `module.zip`. No provider activity or Foundry mutation occurred; the installed v0.3.5.60 module was not updated because this release changes CI workflows and documentation only.

## 2026-09-05 — Post-release v0.3.5.60 creature-feat QA

- While the parent session was paused, the user merged PR #100, moving `origin/main` to `5158902` and publishing **v0.3.5.60**. A fresh independent reviewer tool approved with no findings. A separate read-only reviewer inspected HEAD and adjacent validation but could not execute tests or mechanically diff; that inspection is recorded as context, not as a test result. BrowserOS verified successful PR checks run `33941974897`, successful Auto Release run `33941979908`, and release assets `module.json` and `module.zip`.
- BrowserOS used Setup's module-specific update to change **only SimplyPF2e** from **0.3.5.59** to **0.3.5.60**, launched the `test` world, and joined as GM. SimplyPF2e **0.3.5.60** loaded on PF2e **8.5.0** with no core/system update.
- The actual Generator UI level-4 NPC Preview Plan produced **Goblin Scroll Courier**, showing **19/19** compendium matches, one visible `Route Memory — NARRATIVE ONLY` ability, and correctly labeled **Scroll of Fear (Rank 2)** loot. It cost **25,867 tokens**. The preview made no creature-feat selector request, so it is not end-to-end omission-bearing provider/UI acceptance.
- A separate small direct call to loaded production `selectCreatureFeats`, using a level-4 untrained-courier/Fleet wishlist, the actual 16 class candidates, and the real configured provider, returned `feats: []`, `omitted: true`, and `{ prompt: 1444, completion: 9, total: 1453, estimated: false }`. This live-checks the selector flag; the caller's empty-selection behavior is covered by the production-class local regression, not by a full omission-bearing provider UI run. Total QA spend was **27,320 tokens**.
- Actor count remained **19**, and world items/macros remained **5/5** before and after Preview; the direct selector also observed 19 actors. No Create Actor click occurred and no QA artifacts were removed. Task-owned BrowserOS page **17** retains the preview.
- A brief temporary fetch observer captured only feat response content in the owned tab, forwarded responses unchanged, then restored the original `fetch` and deleted the observer. The preview's extra occult spells and equipment/loot, despite a minimal/no-spellcasting prompt, were not investigated; record this as a separate fidelity observation potentially influenced by advanced controls, not a release regression or proven cause.
- Earlier raw failure responses remain unavailable, so this QA does not prove both historical error causes. Parent's prior local validation had passed all **67 regression files**, **102 syntax checks**, module/localization JSON parsing, and whitespace gates. The new `docs/creature-feat-release-qa` branch is documentation-only from `origin/main`; the earlier auth/review/publication blockers are superseded, not erased.

## Continuation — distinguish optional feat omission from failed grounding

- Resumed from `3b96d7c` on `fix/creature-feat-omission`. Read-only BrowserOS checks of the installed v0.3.5.59 module show Fleet is a level-1 general feat, excluded from the class-only creature catalog, while level-4 Sneak Attacker is offered. Both earlier runs' raw selector replies remain unavailable; do not claim their precise cause is established.
- Found a deterministic contract mismatch: the creature feat selector allows an explicit empty selection but its caller only replaced the wishlist for nonempty decoded feats. A valid `featIds: []` therefore retained ungrounded first-draft names and caused completion failure. Added a local `omitted` result flag derived strictly from that explicit empty reply; the caller now clears only that intentionally declined wishlist. Nonempty all-invalid results, unavailable catalogs, provider errors, and malformed replies retain the existing fail-closed behavior. No feat eligibility expansion, PC change, schema change, or invented mechanics.
- Production-caller regression reproduced the failure before the fix. Actual provider-path regressions distinguish intentional omission from unresolvable picks and preserve bounded retry/token behavior. All 67 regression files, 102 syntax checks, module/localization JSON parsing, and whitespace gates passed. No live provider spend or world-document mutation; actor count stayed 19.
- Independent review remains pending because subagents were uninstalled and no child-agent tool is available. No push, PR, release, deployment, or post-fix live acceptance is claimed. Preserve the post-release documentation on this branch and follow normal review/publication authorization gates.

## Continuation — blank scroll catalogs and narrative match counts

- Live v0.3.5.58 catalogs returned published blank scroll templates as equipment/loot candidates. Actual PF2e 8.5.0 documents showed `system.category: scroll`, `system.spell: null`; fetched PF2e 8.4.1 consumable source confirmed `SpellSource | null`, and the master template explicitly directs creating a scroll by dragging a spell. Worker `8173acb7-700a-4477-8c4a-9344169fbded` added indexed spell data and a nullish-spell scroll filter, preserving the underlying index for spell-grounded building.
- The courier's 12/13 badge counted its labeled narrative ability as a miss. The summary now excludes only explicit narrative rows. Independent reviewer found the caller lost that flag; parent fixed the mapping and replaced manually copied helper tests with actual production preview/summary code, observing red then green. Tests retain unresolved required-item and non-narrative-ability counting.
- Full 66-regression/101-script syntax/JSON/whitespace gates and follow-up independent review passed. PR #99 merged as `d6775ad`; CI and Auto Release succeeded, publishing v0.3.5.59. BrowserOS performed the module-only update and confirmed the loaded version. Live candidate APIs exclude blank scrolls while retaining internal template lookup. Two subsequent courier previews failed closed on unrelated feat grounding (`Sneak Attacker`, then `Fleet`) with no actor-count change (19), so rendered narrative-count live acceptance remains unverified. Stop further provider retries and trace the feat seam next; see HANDOFF.md.

## Continuation — v0.3.5.57 acceptance and scroll preview correction

- Reconciled the stale handoff against git: PR #97 already merged at `f1d1eb5` / v0.3.5.57. BrowserOS confirmed that installed version on Foundry 14.365 / PF2e 8.5.0. The real Items-directory button opens the forge, remains singular across tab switches, and repeated clicks retain one window. No update or world-document mutation was needed.
- Read-only scout `935b3204-c8c2-43a5-a70b-a4148ee2a8a7` traced `Scroll of undefined` to the shared preview mapper reading `.name` from identity-only exact references. Parent verified the source and rejected the scout's additional missing-parenthesis claim (the parenthesis already exists).
- The mapper now uses the selector's original grounded scroll label when an entry lacks a name; named legacy entries retain existing formatting. Reference identity, completion checks, and document creation are untouched. A production-app regression failed with `Scroll of undefined (Rank 2) ×2` before the change and passed afterward; full regression/syntax/JSON/whitespace gates passed.
- The armor observation is model prose, not a module-created activation; no speculative prose removal or mechanics patch was made. Independent reviewer approved; PR #98 merged at `8d777f5` and CI/Auto Release passed, publishing v0.3.5.58. Setup already had that version installed on continuation. Actual loaded-module Preview Plan produced correctly named Fear and Invisibility rank-2 scrolls (21,529 tokens), with no actor creation. The same run's 12/13 match status, extra gear beyond the prompt, and generic scroll-template equipment are separate follow-up observations; see HANDOFF.md.

## 2026-09-05 — Re-expose Item Forge in the Items directory

- Started `codex/item-forge-entry` from public `origin/main` `220741b` / **v0.3.5.56** and carried forward the post-publication/live-QA documentation commits. The forge engine had passed its cancellation, runed-sheet parity, and generated-macro escaping acceptance checks, but ordinary users still had no discoverable UI entry point.
- Extended the existing GM/PF2e directory-button pattern with a shared renderer. The Actors directory keeps its SimplyPF2e Generator button, and the Items directory now gets one idempotent **Item Forge** button that calls the already-protected `openItemForge()` singleton. Player and non-PF2e boundaries remain enforced both at render time and in the public API.
- Expanded the production-import permission regression to cover player invisibility, GM visibility, duplicate-render prevention, localized button identity, click-to-open behavior for both directories, console API access, and wrong-system invisibility/failure. All 65 regressions, syntax checks for all 100 scripts, JSON parsing, and whitespace checks pass locally.
- Real Items-directory lifecycle QA requires the updated module artifact. A temporary attempt to imitate the branch inside the already-loaded **v0.3.5.56** world was discarded as non-evidence; its QA macro command was restored exactly and the temporary hook/button cleanup was run. Branch/PR review and the single intended release must complete before the real Items-menu click test.

## 2026-09-05 — Post-release live QA for v0.3.5.56

- Verified the public module **v0.3.5.56** in the user's logged-in world on Foundry **14.365** / PF2e **8.5.0**. The code audit's schema/source target was PF2e 8.4.1, so this is forward-runtime evidence rather than a replacement source review.
- Cancellation isolation passed in both directions with concurrent Generator and Item Forge requests. Generator cancellation produced only the expected `simplypf2e | generation cancelled` warning while Forge completed. Forge cancellation re-enabled only Forge while Generator continued through later phases and reached its own fail-closed unresolved-equipment error. No wrong-request abort appeared in the console.
- Runed-item prepared parity passed. `+1 Ghost Touch Longsword` was level 4 / 110 gp in preview and on its PF2e sheet; `+1 Slick Chain Mail` was level 5 / 205 gp in both. The armor description unexpectedly mentioned "Custom activation runes" after a no-activation prompt, but the item had only +1 potency and Slick and no activation/rule leak.
- Macro runtime escaping passed twice using actor name `QA <b>Actor</b>`. A healing macro emitted literal markup text in roll flavor. A generated condition macro forced a Will failure, applied frightened 1, and emitted `applied frightened 1 for 1 round to QA <b>Actor</b>.` with the angle-bracket text preserved rather than rendered as HTML.
- Preserved QA actors, tokens, world items, companion macros, and chat cards for inspection. The GM's temporary character assignment and token targets were restored. A discarded cleanup attempt used nonexistent `game.user.updateTokenTargets` and failed before mutation; the corrected per-token cleanup succeeded. Foundry/PF2e itself logged `_drawTargetArrows` `clear` errors during programmatic target setup while still targeting correctly.
- Separate follow-up observations: one early creature preview displayed `Scroll of undefined (Rank 2)`, and the armor description contradicted the no-activation prompt. Neither invalidated the audited cancellation, macro-escaping, or runed-sheet fixes; both need focused triage before deciding whether to patch.

## 2026-09-05 — Audit fixes (local `codex/audit-fixes`)

- Followed the audit finding set against public `origin/main` `eb779209` / **v0.3.5.55**, then published the combined fix as PR #96 / merge `220741b` / **v0.3.5.56** so only one main push and release occurred. No provider request, Foundry/VPS mutation, live QA, or change to the user-owned `.claude/` directory occurred.
- Replaced the module-global generation abort slot with an `AbortSignal` owned by the individual `SpfApp` run. `_beginProgress()` supplies that signal to every generation/item-forge AI call, `requestJSON`, and `requestCompletion`; request retries retain it. Connection and `/models` probes deliberately remain independent. A provider-path regression starts two pending calls with distinct controllers and proves cancellation A leaves B pending, then cancellation B independently terminates B as non-retryable `Cancelled`.
- Macro templates cannot import module helpers at execution time, so they now carry one fixed runtime HTML-escape helper. Actor names are escaped only at the final ChatMessage/roll-flavor boundary; already escaped item/effect/duration handling remains intact. The regression executes generated condition/heal macro bodies with hostile target/acting names and proves the emitted chat/flavor text contains entities rather than raw tags.
- Runed item assembly now separates PF2e source data from transient preview metadata. It clones real base/rune documents as before and returns `{ itemData, preview: { priceGp, level } }`, preserving base `system.price`/`system.level` rather than overwriting them with derived totals. This matches PF2e physical item preparation: `src/module/item/physical/document.ts` calls `computeLevelRarityPrice(this)` and writes `system.level.value`/`system.price.value`; for an ordinary nonspecific runed item, `physical/helpers.ts#computePrice()` excludes base price once rune value is positive. The base catalog now requests and carries `system.specific`, excluding non-null specific weapons/armor before the model can select them. The focused mock-compendium regression confirms a +1 striking longsword retains 1 gp / level 0 source values while preview is 100 gp / level 4.
- Added `jq empty module.json lang/en.json` to reusable `release.yml`'s `verify` job so workflow-call auto-release, tag, and manual release all use the same JSON gate. PR checks and Auto Release run `33896508187` passed; the release verifier executed the new JSON step and published the `module.json` and `module.zip` assets. Live Foundry behavior remains unverified.

## 2026-09-04 — T2 UI polish (cancel, apply chrome, phase, last-run)

- Architect-approved Prism memo items 3, 4, 5, 7 on `origin/main` `a2bfa6f` / **v0.3.5.51**. Branch `cursor/t2-ui-polish-0918`, PR #92. Do not merge to `main`.
- Cancel sits on the shared progress chrome (outside the disabled fieldset). It aborts the in-flight provider `AbortController` and is classified as `Cancelled`, never Timeout, and is not retried. Partial preview state is dropped; the form re-enables. Foundry create/PC apply is not armed, so a write is not left half-done. No generation toasts.
- PC apply keeps the step card/bar; `busyMessage` is the detail on that chrome instead of swapping to spinner-only.
- Progress card carries thinking vs writing (and cancelling) phase classes. Sheen/spinner yield to `prefers-reduced-motion`. Percent still caps below 100 during a run; cancel does not mark steps done. Errors remain below the bar.
- Compact last-run cost on the provider strip (`last: N tokens` / `last: ≈ N tokens`). Estimated totals stay coarsened and keep `≈`. Snapshot persists across the next busy state until a new run finishes.
- Local verification: all 63 regression files pass. Live Foundry click-through of Cancel, apply chrome, phase motion, last-run, and error visibility is still required.

## 2026-09-04 — Coin loot as sheet currency

- JT: generating loot put gold coins on the sheet as custom treasure. `parseCoins` already mapped "Gold Coins"/"gp"/etc. to "Gold Pieces", but production `exactContent: true` never resolved those module-built lines (no AI candidate), so `buildLootItems` called `customTreasureItem` — a treasure without PF2e 8.4.1 `system.category === "coin"`.
- Fetched `foundryvtt/pf2e` **pf2e-8.4.1** (not master). `TreasurePF2e#isCoinage` is `this.system.category === "coin"` (`src/module/item/treasure/document.ts`). `stackGroup === "coins"` is migration input (`TreasureSystemData.migrateData`). `ActorInventory.addCurrency` (addCoins alias) clones `coinCompendiumUuids` from `pf2e.equipment-srd` (`src/module/actor/inventory/index.ts`).
- Fix: always resolve published coinage in `resolveLoot` (exactContent does not apply), clone it in `buildLootItems`, fail closed on unknown denominations or missing coinage docs. `applyTreasureBudget` padding keeps the official Gold Pieces entry. Did not call addCurrency after Actor.create: that API clones the same documents, and embedding them at create time stays inside the existing transaction/expectedItems path. Shared by NPC loot and PC starting wealth.
- Rebased onto `origin/main` `cedbd9e` (PR #90 / **v0.3.5.50**) after Sentinel reported CONFLICTING / behind main. Docs conflicts only; coin clone hunks unchanged. Branch + PR only; do not merge to `main`.

## 2026-09-04 — Smooth progress + clearer token estimates

- JT: the loading bar jumped between equal step buckets and felt inaccurate; token estimates should be more honest.
- Root cause: `_setStep` assigned `((done + 0.5) / n) * 100` (equal weights, mid-step snap) and re-rendered the whole app, remounting `.spf-progress-fill` so the existing width transition never ran. Stream ticks only patched the detail line. Char/4 estimates were shown at ones precision.
- Fix: `progress.mjs` weights steps from `taskMaxTokens` (concept > selectors; encounter members cover the creature pipeline; local match/templates/assemble stay a small display share). Intra-step fill is phase-only (thinking → writing); streamed chars never invent % of an unknown final length. The sheen is the within-step motion. `app-base.mjs` patches the existing fill/percent/detail/step icons after the first render. Unknown keys no-op. Live copy keeps `≈` until provider `usage`; spell focus vs selection is a detail sub-label. `tokens.mjs` prefers complete provider usage, labels fallbacks, coarsens displayed estimates, and treats total-only blocks as exact totals without a fake split.
- Local verification: all 62 regression files pass (`progress.test.mjs`, `tokens.test.mjs`, plus usage/uistate/layout contracts). No live Foundry QA. Published on `main` as PR #90 / **v0.3.5.50**.

## 2026-09-04 — Unify generator dice across four modes

- JT asked for a random-gen button on every mode. Monster/NPC already shared `#onGenerateRandom`; Encounter had a duplicate `#onGenerateRandomEncounter`; Character had the pipeline (`#generatePC(true)` already accepted `isRandom`) but the template gated the button off because `randomBrief()` was creature-flavored.
- One `spf-random-button` now sits in the generate row for all four modes. It always calls `#onGenerateRandom` → `#runGeneration(true, { create: false })`, so dice remains preview-only and still ignores the typed prompt. `randomBrief(mode)` keeps the creature sentence for Monster/NPC/Encounter and adds a lean person-oriented adventurer brief for Character (flavor words only; not class/ancestry picks). Mode-specific i18n tooltips; no Advanced-control randomization.
- Local verification: `node --check` on touched `.mjs`; new `presets.randomBrief.test.mjs` plus `ui.layout.test.mjs` dice-row contract; all 60 `scripts/*.test.mjs` pass. Live Foundry click-through of all four dice buttons is required. Branch + PR only; do not merge to `main`.

## 2026-09-04 — Client-side AI connection bank

- JT needed to keep a working DeepSeek connection and a second custom OpenAI-compatible endpoint without re-entering URL/key/model each time. Added named connection profiles in a client-scoped `providerBank` setting (keys stay browser-local, never world-synced, never shown as plaintext in ordinary settings). Each profile stores display name, API root, model, key, and that profile's exact-URL binding.
- The four existing live settings remain the active connection that `getProviderRequestConfig()` / generation use. Switching a profile writes it into those settings (restoring that profile's binding after the key write); saving writes live settings back onto the active profile. Creating a profile never copies another endpoint's secret. The last profile cannot be deleted.
- Existing single-endpoint worlds migrate into one inferred-name profile on first setup/save. Provider Setup lists/selects/creates/renames/deletes profiles; Save & Test / Save & Authorize still bind the active profile. Generator and item-forge headers show the active name and offer a one-click switch once two profiles exist.
- Rebased onto `origin/main` `d47bbcb` (PR #87 / **v0.3.5.47**). Docs conflicts only; JSON matching-brace parse from #87 kept. Local verification: all 59 regression files pass, including expanded auth/setup coverage for migration, CRUD, active switch, and per-profile key binding. Empty remote keys, mixed-content HTTP providers, and unbound keys still fail closed. No live Foundry QA.

## 2026-09-04 — AI JSON parse / BadJson diagnostics

- Live QA surfaced the exact `SIMPLYPF2E.Errors.BadJson` string on NPC Generate/Preview. Audit found `parseConceptJSON` used first `{` + last `}`, which rejects a complete object when trailing prose contains `}` and can also hide truncation as BadJson when `finish_reason` is `max_tokens`.
- Fix: string-aware matching-brace extract of the first complete object; unwrap a JSON-encoded object string once; join Chat Completions text-part arrays; read SSE `message.content` when `delta.content` is absent. Length truncation still fails closed before parse (`length` and `max_tokens`), including complete-looking prefixes. Reasoning-channel JSON is not accepted; empty content with reasoning uses `ReasoningWithoutJson`. Retry-once still fires with the existing retry prompt plus truncated console diagnostics (finish_reason, content length, preview). `response_format` is unchanged except the existing named compatibility drop.
- Rejected: repairing truncated JSON, scanning into unmatched `{` to salvage nested objects, treating reasoning traces as the answer.
- Local regressions expanded in `ai.parseJson.test.mjs` and `ai.usage.test.mjs`. Live NPC Generate/Preview against JT's provider is still required. Published on `main` as PR #87 / **v0.3.5.47**.

## 2026-08-31 — Post-v0.3.5.45 generator selection fixes (local branch)

- Reproduced the reported double-highlight in the live Foundry UI. The selected mode's checked state was correct, but the application re-render moved focus to the first visually hidden radio (Monster), whose `:focus-within` outline looked like a second selection. The local fix restores focus to the checked mode after rendering and has a production-application regression.
- Live logs proved only the all-at-once feat/equipment completion failure; they did not capture the provider's raw selector payload. Code tracing separately found and reproduced a compatible shared-decoder failure: a response can satisfy the JSON schema while putting an exact displayed candidate name in the `id` field, which the old decoder rejected before the exact-name catalog path. The decoder now accepts only an exact, unique name from the already-issued catalog when the opaque ID does not match. Equipment/loot also preserve the prompt's existing allowed fundamental-rune prefix only when its base is an exact issued item. Fuzzy names, ambiguous names, invented rune bases, unissued references, and fabricated pack/document identities remain rejected. Post-release live QA must confirm this was the provider's actual response shape.
- Branch `codex/fix-generator-selection-completeness` contains the two focused code changes and production-path regressions. All 59 regression files, touched-file syntax checks, and `git diff --check` pass. No GitHub or VPS write occurred; live proof awaits publication.

## 2026-08-31 — Live generation contract fixes (local branch)

- Connected read-only to the user's running Foundry world and observed a level-6 Monster generation through the public v0.3.5.44 UI. Spell selection raised `ReferenceError: focusSpells is not defined`; ability selection then spent its bounded retry returning `abilityIds` to the PC feat validator, which required `picks`. Completion failed closed on unresolved Grab, and no Tidespore Oracle actor existed afterward, confirming no world write or rollback leak.
- Traced both failures to the production code. `selectSpells()` already returns grounded `focusSpells`; the generator omitted it from destructuring. Creature feats were assigned the ability task while creature abilities were assigned the PC feat task, and `ABILITY_SELECTION` had no response-shape rule. The fix retains `focusSpells`, gives creature feats a dedicated 1,536-token deterministic task, assigns abilities to their intended task, and validates the exact `featIds`/`abilityIds` arrays before local opaque-ID resolution.
- Added a provider-path regression that drives both real selectors through mocked OpenAI-compatible responses, verifies exact reference retention/deduplication, response-key rejection plus one bounded retry, and task limits. Existing spell and response validation tests cover the retained focus-spell return. Full local verification passes: 59 regression files, syntax checks for all 91 `scripts/*.mjs`, `module.json`/`lang/en.json` parsing, and `git diff --check`.
- Work is isolated on `codex/live-qa-generation-fixes`. The user's current request authorizes branch push, PR, and merge; the automatic release and post-release VPS rerun still require verification. No VPS files, world documents, settings, or provider credentials were changed by the agent.

## 2026-08-31 — PR #83 merged and v0.3.5.43 published

- With explicit user authorization, pushed `codex/one-click-grounding`, opened PR [#83](https://github.com/simplyjaytea/simplyPF2e/pull/83), waited for GitHub's `verify` check, and merged it into `main` as `07a369fc8589c263e537fe65024b704161dba0d7`.
- The repository's Auto Release workflow #48 completed successfully and published [v0.3.5.43](https://github.com/simplyjaytea/simplyPF2e/releases/tag/v0.3.5.43), including `module.json` and `module.zip`. The downloaded manifest reports module version `0.3.5.43` and Foundry compatibility minimum/verified `14`.
- The merged source contains the uniform one-click intents, exact compendium identity gates, completion manifest and source preflight, transactional creation, PC HP/spell/signature/skill completion, proficiency-aware loadout, Free Archetype fail-closed handling, exact native class-path staging, and the conservative empty-prerequisite PC feat gate. Complete-only PC support remains Fighter-only; no live Foundry/VPS deployment was performed.
- Documentation was refreshed after publication so README, CLAUDE, and HANDOFF now point to `origin/main` `07a369fc`, release v0.3.5.43, the recoverable topic branch, and the next prerequisite-evaluator slice. The follow-up documentation branch must use the normal PR path if published.

## 2026-08-31 — Paused at a clean one-click milestone (local branch)

- Stopped implementation on `codex/one-click-grounding` at `1d9d755` (`docs: record PC prerequisite gate`), based on `origin/main` `004bffc` plus the completed local PC stack. No push, PR, merge, release, provider request, VPS mutation, or Item Forge work occurred. The user-owned untracked `.claude/` directory remains untouched.
- The safe stopping point is exact native class-path staging plus a conservative PC prerequisite catalog gate. Rogue/Investigator level-one Racket/Methodology bridges are staged only from enabled exact Class Features sources and only for closed static ChoiceSet paths; all other native grants remain intact. Complete-only class support intentionally remains Fighter-only.
- PC feat candidates on the complete path now require an explicit empty `system.prerequisites.value` array. Dependent, malformed, and unreadable prerequisite data is excluded before provider selection, and empty entitlements remain visible so the completion manifest blocks rather than silently dropping a slot. NPC and legacy callers remain permissive. This is not ordinary staged-actor prerequisite evaluation.
- Documentation was refreshed in `README.md`, `CLAUDE.md`, and `HANDOFF.md` with the exact stopping point, branch/commit, verification, unsupported boundaries, and next task. The next implementation slice is a fail-closed prerequisite evaluator against the staged actor, followed by independent schema review, class qualification, and live grant-chain QA.
- Final local verification at pause: all 59 regression files pass; every `scripts/*.mjs` passes `node --check`; `module.json` and `lang/en.json` parse; and `git diff --check` passes. This evidence is local only and does not replace live Foundry acceptance.

## 2026-08-31 — Exact Rogue/Investigator class-path staging (local branch)

- Added a narrow native bridge for mandatory level-one Rogue and Investigator choices. The builder clones only the real enabled-source Racket/Methodology feature, links it to the new class, removes only its matching `class.system.items` entry, and leaves every other class grant on PF2e's native pipeline.
- The bridge's dynamic tag query is replaced before creation with opaque, exact enabled Compendium candidates. Only closed paths are accepted: static non-predicated ChoiceSets with no downstream ChoiceSets beyond PF2e GrantItem's real one-level `preselectChoices` seam. Omitted, dynamic, predicated, unsupported, or unanswered paths block before `Actor.create`; no Rule Elements were authored.
- Added the additive **Class Features** source category (default `pf2e.classfeatures`), source UI label, and exact staging regressions. Independent review correctly kept the complete-only registry Fighter-only: ordinary PC feat prerequisites are not yet validated against a staged actor, so the new bridge is not a safe basis for qualifying Rogue/Investigator. Wizard additionally needs class-owned spellbook/curriculum support. Local verification at that commit passed 58 regression files, all script syntax checks, module/localization JSON parsing, and whitespace validation; the later prerequisite-gate commit raises the current total to 59. Live Foundry grant-chain QA and publication remain open.

## 2026-08-31 — Fail-closed PC prerequisite catalog (local branch)

- PC feat candidate selection now accepts only an explicit empty `system.prerequisites.value` list when resolving one-click character slots. Dependent, malformed, or unreadable prerequisite data is excluded before the model sees it; NPC/legacy candidate callers retain their prior behavior. This is a conservative source gate, not a staged-actor prerequisite evaluator, and the Fighter-only complete registry remains unchanged.
- Added a focused regression for empty/dependent/missing prerequisite data. The local suite now passes 59 regression files, all script syntax checks, module/localization JSON parsing, and whitespace validation. No provider, GitHub, VPS, or live Foundry action occurred.

## 2026-08-30 — Proficiency-aware PC equipment readiness (local branch)

- Added a post-native PC loadout step on `codex/one-click-grounding`, with no GitHub/VPS/provider action. It handles only the exact equipment sources created in the active transaction, never existing actors or similarly named/native items.
- Source was checked against PF2e 8.4.1 `actor/character/helpers.ts`, `physical/usage.ts`, and item documents. The planner mirrors prepared weapon category/group/base/equivalent/synthetic ranks and armor category/synthetic ranks; it readies one proficient armor and up to two held hands, keeping untrained or conflicting equipment stowed. A review card exposes those unsupported cases instead of quietly wielding invalid gear.
- The equipment write occurs after native grants and before HP/final commit, so failure rolls back the new actor. Generated clones carry transaction-local embedded IDs, preventing a same-source native grant from receiving the update. Compatible generated ammo is selected only for non-repeating reload-0 weapons using PF2e's native `isAmmoFor`; reloadable/repeating weapons retain native subitem loading. Investment, container placement, and item activation state remain untouched. Added pure planning/application regressions; all 56 regression files, every script syntax check, JSON parsing, and whitespace validation pass. Live Foundry equipment/derived-strike behavior still needs QA.
- Corrected Free Archetype feat placement from the ordinary `class-<level>` group to PF2e 8.4.1's actual `archetype-<level>` group. The source/category stays `class`; only its native character-sheet slot changes. Added pure slot and mocked creation-path regressions. Independent schema review approved the exact 8.4.1 `CharacterFeats`/`FeatGroup` behavior. Because prerequisites are display text without a generic staged-actor eligibility API, the complete-only preflight now refuses level-2+ Free Archetype requests before provider spend; level 1 has no variant slot. Full prerequisite-graph support remains open.

## 2026-08-30 — Exact post-create transaction boundary (local branch)

- Continued on `codex/one-click-grounding`; no push, PR, merge, release, provider request, VPS action, or change to user-owned `.claude/`.
- Replaced post-create name/type matching with an in-flight exact-item contract. Builders return the sources supplied to Foundry only for the active transaction; persisted compendium clones are matched by `_stats.compendiumSource`, so same-named documents/native grants cannot satisfy an exact pick. NPC feat-to-action conversion preserves its source UUID. Module-built/narrative items remain structural type/name checks because they intentionally have no source document.
- Spell survival now also requires that each persisted spell location reference an actual persisted `spellcastingEntry`. Ordinary and focus entries are module-built completion records; the focus entry is recorded even for a focus-only PC. This is verification, not a second PF2e derived-stat or feat-prerequisite engine.
- Creation now has an explicit commit boundary across NPC, PC, and encounter flows. All writes/verification succeed and drafts clear before notifications, sheets, or result rendering. Pre-commit errors roll back only newly created documents; if actor/folder deletion fails, the draft is discarded and the surviving document is reported so retry cannot duplicate it. PC-builder rollback failures carry the survivor to the generator for the same treatment.
- Independent review initially caught name-only verification, unsafe retry after cleanup failure, missing spellcasting infrastructure, and presentation before commit; all were corrected. A second review approved, including the focus-only follow-up. Regression coverage adds exact duplicate-name/source rejection, casting-entry links, failed cleanup/non-retry, stranded native creation, and commit-before-presentation behavior. All local tests, script syntax checks, module/localization JSON parsing, and whitespace checks pass. Broader monster/NPC/encounter lifecycle mocks and live Foundry transactional behavior remain QA work.
- Made bestiary Actor scaffolds a creature/encounter source-preflight requirement. The existing exact matcher now always selects a deterministic closest NPC from the configured source packs, prioritizing shared traits, size, and level but falling back to level/size for an unfamiliar valid trait set. It remains token/prototype scaffolding only; no source actor statistics/items are copied. Readiness and matcher regressions cover the required-source block and fallback.

## 2026-08-29 — One-click workflow foundation (local branch)

- Started `codex/one-click-grounding` from the completed PC stack. No push, PR, merge, release, deployment, provider request, or VPS action. Baseline verification passed all 46 regression files, all script syntax checks, localization/module JSON parsing, and diff whitespace checks.
- Replaced the old ambiguous Single intent with Monster and NPC. They remain on the shared PF2e NPC engine, but the existing concept call now receives an explicit combat-ready monster/NPC framing. Primary Generate now runs generation/validation and creates the result; Preview Plan intentionally preserves the former no-write review flow. The random button remains preview-only.
- Hardened creation boundaries: a newly created creature draft clears before sheet presentation, and a failed encounter rolls back each actor and the newly created folder before leaving the plan retryable. The remaining one-click roadmap is intentionally not claimed complete: exact candidate IDs, completion manifests, required-source preflight, full actor-pipeline closure, uniform result card, and live Foundry acceptance are still in flight.
- Continued the grounding seam for spell, equipment, loot, ABCs, and feats. Candidate lists now hold opaque model IDs and private exact `{packId,_id}` references. Prompts request IDs, then selection preserves the issued reference to resolve the exact offered document rather than name-searching again. A transitional name path exists only for old ID-less callers; ID-bearing catalogs reject ambiguous legacy names. All regression files still pass. Focus/bestiary categories and completion enforcement remain unimplemented.
- Added a pure, transient completion manifest and wired it before creature, encounter, and PC plans are retained/created. Unresolved required content now aborts generation; coins and spell scrolls are explicitly module-built, and custom creature abilities are labeled non-required narrative. Added direct regression coverage plus production-generator wiring. The result-card/readiness presentation and focus/bestiary grounding remain next.
- Added source readiness/preflight. The generator checks AI provider readiness and enabled required compendium categories before requests, shows a separate labeled Compendium Content row with effective pack count/actionable configuration, and removes the duplicate unlabeled source gear. The category preflight has direct tests. No live Foundry request occurred.
- Added the shared post-create result card for every generator mode. It retains the committed result only after successful writes, offers Open Sheet where an actor is available and Generate Another, and never recreates an actor through presentation. PC review snapshots coexist with it. All regressions/syntax/JSON/diff checks pass.
- Added an explicit complete-only PC registry: Fighter, Rogue, Investigator, and Wizard are the only classes offered to one-click character generation. This prevents unsupported classes from silently using generic or incomplete paths. The missing-qualified-class failure occurs before provider spend and has direct regression coverage.
- Replaced fixed post-key ability ordering with locally validated optional concept preferences. The class key ability stays mandatory; remaining legal ancestry/background and level boosts honor unique model preferences without accepting any numerical allocation. Added pure normalization coverage and re-ran the full local suite.
- Added bestiary Actor source discovery/configuration and deterministic scaffold selection by exact trait/size/level evidence. Creature creation imports only the source actor's art/prototype-token structure, retaining module-owned stats/items; no source mechanics are copied blindly. Added a focused scaffold test. This requires live Foundry token QA.
- Continued on the same unpublished branch: focus spells now join the existing grounded spell-selection response through opaque IDs and retained exact `{packId,_id}` references. The resolver fetches that exact selected document and confirms it is a focus spell before embedding; normalization preserves the reference across NPC/PC handoffs. No extra provider call was added. Focus remains class-feature legality dependent and needs live casting/pool QA.
- The transient completion manifest now drives the post-create card's count-only grounding summary and preserves the generation token report after preview data is cleared. The summary is never persisted to actor flags. This also fixed a generation-failure error handler that tried to reference an actor that did not exist, masking the actual failure.
- Narrowed this local line's manifest/README compatibility target to Foundry 14 / PF2e 8.4.1; it is a support boundary, not new live-QA evidence. Expanded pure/production-static regression coverage for focus ID selection, reference-preserving normalization, completion summaries, completion-card content, and the error boundary. No GitHub/VPS/provider action occurred.
- Reworked the generator around the one-click hierarchy: prompt and level are always visible; preset/rarity/treasure/spellcasting and encounter controls are under native Advanced options. Replaced four confusing preset icons with one labeled Manage Presets action. The manager owns New/Edit/Duplicate/Export/Delete/Import and refreshes its generator after mutation. Removed now-dead generator action handlers. Full regression/syntax/JSON/whitespace checks still pass locally.
- Hardened the character spell-plan completion boundary: when exact selection fails, known planned cantrip/ranked slots stay empty, and the manifest produces one blocking unresolved record per missing slot. The prior draft-name/fuzzy-resolution escape hatch was removed, so complete-only creation cannot produce a caster with an incomplete published plan.
- Extended that strict boundary to final one-click resolution: PC ABCs, spells/focus spells, equipment, and loot, plus creature spells/focus spells/equipment/loot, require the opaque exact candidate reference retained from their selection pass. Older pre-selection/legacy callers remain permissive. Failed grounded content now blocks in the completion manifest instead of being fuzzy-matched after selection. Canonical currency is module-built. Scrolls now use a bounded, tradition-neutral exact spell candidate slice in the existing loot request and carry that selected source reference through module-built creation; the source preflight requires spells even for a non-caster. Added focused strict-boundary coverage and restored canonical `Gold Pieces` recognition in the manifest.
- Added the missing creature-feat grounding pass. A concept with class-like feat ideas receives up to three selected IDs from a bounded set of real class feats at or below the creature level; the retained source reference is required at final creation. Preflight now requires feat sources for every intent. This removes the old post-selection name lookup; feat prerequisites/context are still a separate, uncompleted validation layer.
- Added the equivalent bestiary-action boundary. The model now chooses only opaque IDs from a bounded bestiary-action catalog; selected documents are cloned directly. Unselected bespoke ability flavor is explicitly marked as a passive narrative-only action and cannot carry module-authored action economy, traits, or rules; it never takes a post-selection name lookup. The concept prompt no longer solicits arbitrary custom mechanics; it reserves custom entries for narrative flavor. Ability sources preflight before provider spend. This does not yet offer bounded module templates for bespoke mechanical abilities.
- Hardened exact references beyond pack membership: candidate records now mint an in-memory reference identity, and strict resolvers require that identity as well as the permitted source pack. A model cannot pass a guessed/lookalike `{packId,_id}` reference. Legacy/pre-selection lookup preserves its separate permissive behavior. The exact-content regression now derives valid references from real issued test candidates and proves that plain lookalikes fail.
- Hardened NPC strike assembly against PF2e 8.4.1's real Melee schema. Model-provided damage types, NPC attack traits, and attack effects now pass through the runtime choice catalogs; invalid values drop before actor creation. Valid `thrown-N` traits choose the same range PF2e will apply during data preparation. Ranged attacks write `system.range: { increment, max: null }` with the source-defined 5–500-foot, five-foot-step bounds. The prior generated `range-increment-*-feet` trait was a legacy migration encoding rather than a valid current document trait. Independent review caught the initial damage check using the full static list rather than the installed catalog and confirmed the correction plus thrown reconciliation against 8.4.1 source. A production-path mock regression covers rejection and emitted data. No live sheet/range QA or publishing occurred.
- Closed the remaining strict PC-feat resolver escape. The one-click caller now requires a locally issued exact candidate from the same feat slot for model picks and deterministic missing-pick fallback; a lookalike `{packId,_id}` reference or a candidate from another slot cannot trigger a fuzzy duplicate-name match. Legacy/pre-selection callers retain their direct allowed-pack reference and name lookup behavior. Independent review caught that compatibility detail, it was restored outside strict mode, and approved the result. The exact-content regression exercises a forged source reference, untrusted labels/deduplication, and the legacy path. No live/provider/GitHub/VPS action occurred.
- Created an isolated local `codex/release-json-validation` branch from `origin/main`, commit `7e00930`, to add `jq empty module.json lang/en.json` to `release.yml` before release build. No push, PR, workflow invocation, tag, or release occurred; its GitHub run remains required before it can be considered a trusted release gate.

## 2026-08-28 — Character Skill Completion (local branch)

- Implemented the approved concept-driven training/progression plan on `codex/character-skills`, based on `edad6a9`. No push, PR, merge, release, deployment, live/provider call, Item Forge work, or modification to user-owned `.claude/`.
- Added validated ordered core skill slugs to the existing concept prompt, preserving older responses and token accounting. Missing/partial preferences use labeled key-ability defaults. The module alone assigns numeric budgets/ranks. The new pure allocator consumes real class `skillIncreaseLevels.value` rather than the old universal round-robin schedule and processes each advancement with its own rank cap. Source checks: [master class data](https://raw.githubusercontent.com/foundryvtt/pf2e/master/src/module/item/class/data.ts), [8.4.1 class schema](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/item/class/data.ts), and [class rules journal](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/packs/pf2e/journals/classes.json). Rogue/Investigator accelerated schedules are native data, not name-based special cases.
- Initial training uses the signed native Int allowance, with direct duplicate class/background replacements added outside the negative-Int clamp. The [Player Core skills rules](https://2e.aonprd.com/Rules.aspx?ID=2134) distinguish replacement trained proficiencies from redundant higher-rank benefits. No extra feat/subclass allowance or arbitrary Lore replacement is inferred. Native [character preparation](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/actor/character/document.ts) exposes integer pre-RE `.abilities.int.base`, including apex. A detached level-1 ABC/heritage clone establishes initial availability; later extra Int training is conservatively delayed to creation level.
- Native [AELike preCreate](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/rules/rule-element/ae-like.ts) and skill predicates make zero-skill embedding unsafe. Provisional purchased ranks plus fixed/native rank floors are seeded first. After actual native creation, observational clones clear only still-owned seed sources; allocation events are reconciled and projected before writes. Native source changes and higher ranks are preserved. Uncertain floor timing is not treated as available at earlier levels, and non-upgrade/injected transforms are excluded. The original grant/RE lifecycle remains intact; no REs were hand-authored or replayed.
- Independent review caught: refunding Expert while retaining an earlier Master dependent on it; allowing later Int training to support earlier increases; omitted fixed-rank roll-option seeding; unresolved AELike log paths bypassing transform detection; replacement training consuming a pinned training increase; and guaranteed duplicate replacements being lost under negative Int. All were fixed and given regressions. The reviewer subsequently passed 30,000 generated reconciliations checking exact +1 events, monotonicity, and budget accounting and approved the implementation.
- Skill/Lore writes now precede HP/language finalization inside the new-actor rollback boundary. The internal builder returns `{actor, skillReport}`; the generator clears the committed draft before rendering the report. Preview shows priority order, and a dismissible in-memory snapshot shows resulting ranks, counters, and unsupported cases. No migration, persisted report flag, extra AI call, new setting, or public API change.
- Added production request, pure allocation, and production builder tests; extended production generator and static UI tests. Coverage includes levels 1–20, normal/accelerated schedules, malformed schedules, signed Int, overlapping grants, duplicate fixed training, late availability, background Lore, injected native transforms, source ownership, failed writes/rollback, and duplicate-creation protection. Real Foundry cloning, predicates/grants, rendering, and derived stats still require the HANDOFF VPS checklist; local mocks/static tests do not prove those behaviors.
- Final local verification passed: all 46 regression files, syntax checks for every `scripts/*.mjs`, parsing of `module.json` and `lang/en.json`, and `git diff --check`. No publication or live acceptance is claimed.

## 2026-08-28 — Native-choice review and safe post-creation presentation (local branch)

- Finished the active feature on `codex/pc-spellcasting`, following `a88daa8`, then updated the project brief and handoff. No push, PR, merge, deployment, or Item Forge work; user-owned `.claude/` remains untouched.
- Confirmed native ABC grants re-fetch/clone their feature documents internally, so the existing source preselection mechanism cannot generally reach them. No lifecycle overrides or speculative actor-flag inputs were added. Sources: [ABC grants](https://raw.githubusercontent.com/foundryvtt/pf2e/master/src/module/item/abc/document.ts), [ChoiceSet master](https://raw.githubusercontent.com/foundryvtt/pf2e/master/src/module/rules/rule-element/choice-set/rule-element.ts), and [installed-version ChoiceSet](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/rules/rule-element/choice-set/rule-element.ts). The constructor reads selections from rule sources; flags are outputs. Suppression/predication can skip prompts, and dismissal can set ignored without rejecting creation.
- Added pure `reviewUnresolvedChoices` over final embedded items. It excludes explicit optional choices, suppressed feats, and recorded string/finite-number/plain-object selections. Missing/invalid selections remain review hints, including ignored/predicated rules; unreadable items mark inspection incomplete. It never mutates items, evaluates predicates, or proves a choice is required.
- Generator shows an escaped, dismissible snapshot with the exact created actor and an Open Character action. It explicitly warns that edits do not refresh the report and that this is not full character validation. No persistent world flags, automatic repairs, or deletion. Zero hints does not display “all complete.”
- Fixed a related duplicate-creation risk: successful creation now clears the draft before sheet/notification/report presentation. Sheet and final report-render failures receive “already created” guidance; genuine creation errors retain the draft. Independent review caught the final-render edge; it was fixed and retested before approval.
- All 43 regression files, all module syntax, module/localization JSON, and diff checks pass. New tests cover pure inspection boundaries and the unmodified production app's generation/create/review actions using mocked imports/platform (including sheet/report failure, unreadable collection, missing actor, dismiss, no-hints success, and genuine creation failure). UI escaping/wiring is static-tested. Real Foundry rendering, grant outcomes, and casting still need the handoff's VPS QA after authorized publication.

## 2026-08-28 — Ordinary signature spells and rank-ten repertoire (local branch)

- Continued locally after `b72c63d`; no publishing authorization. Sol researched and independently reviewed; Terra implemented the plan/native-flag seam; root integrated AI selection, candidate reservation, preview feedback, boundary corrections, and regression tests.
- The [shared Signature Spells feature](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/packs/pf2e/class-features/signature-spells.json) starts at level 3, chooses one spell per learned rank, and has no native ChoiceSet RE (`rules: []`). Native [spell location data](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/item/spell/data.ts) already supports the signature boolean; [spell collection rendering](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/item/spellcasting-entry/collection.ts) expands it across legal ranks from the original base rank. Master source agrees. We set that flag only after validating the real source and ordinary class eligibility, not by authoring rules or overriding casting.
- Research exposed a separate previous-pass bug: slot counts are not always repertoire counts. [Magnum Opus](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/packs/pf2e/class-features/magnum-opus.json), [Oracular Clarity](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/packs/pf2e/class-features/oracular-clarity.json), and [Bloodline Paragon](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/packs/pf2e/class-features/bloodline-paragon.json) add two common rank-ten repertoire spells but one slot. `pcSpellPlan` now owns distinct slots/picks/signature ranks, reused by generation and creation. Signature eligibility includes learned rank ten; common lower-base spells learned heightened are legal too.
- Existing PC selectSpells call now accepts signature/regular enum designations alongside exact names and rank enums; no extra call. Invalid or conflicting designations retain regular spells, never first-option guesses. Both AI selection and actual document creation check rank-ten common rarity; review caught that the initial builder check only rejected a rare signature marker, not the rare repertoire spell itself. The corrected source check runs before counting picks. Candidate sampling reserves common ranked options within the same hard cap, avoiding rare exact-name starvation.
- Preview labels planned signatures and displays selected/eligible counts with escaped status text. This does not claim actual source acceptance or live casting success. Ordinary signatures are implemented only for the three publication-qualified spontaneous classes, not prepared/legacy/complex profiles or bonus-signature feats. Subclass-dependent casting remains incomplete.
- Final independent Sol review approved with no actionable findings. All 41 regression files, all module syntax, JSON parsing, and diff checks pass. New pure plan tests cover all three spontaneous and four prepared profiles; expanded production AI/builder tests cover level gates, enum validation, collisions, stale flags, heightened learned signatures, source rarity, two rank-ten picks/one slot, prepared/unknown denial, and candidate starvation. No real provider/live Foundry requests. VPS QA must check virtual rows, downward/upward casting, correct slot use, and preview/native choice consistency.

## 2026-08-28 — Class-qualified PC spell plans (local branch)

- Continued locally on `codex/pc-spellcasting`, with publication still unauthorized. Terra implemented class profiles and native spell embedding; root integrated the existing AI spell selector, bounded candidate reservation, preview notices, and regression coverage. Item Forge remained deferred.
- Confirmed two production gaps: every PC used spontaneous mode and the same 2/3 slot approximation; PC embedding discarded assigned heightened ranks. [PF2e class data](https://raw.githubusercontent.com/foundryvtt/pf2e/master/src/module/item/class/data.ts) defines class spellcasting as starting proficiency, not native slot derivation. Replaced the misleading RE-derived-slot comment. Seven Remaster Player Core / Player Core 2 classes now use source-qualified profiles. Sol independently compared every level of all seven profiles with the [8.4.1 Classes journal](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/packs/pf2e/journals/classes.json): all 140 base-table rows agree.
- Prepared plans assign real embedded IDs to `slots.slotN.prepared`, including five distinct cantrips when picks are available. Explicit repeated ranked picks share the real spell source across slots/ranks. Native [SpellPrepData](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/item/spellcasting-entry/data.ts) is `{id, expended}`; native preparation pads unused positions, so incomplete plans stay empty rather than guessing. Spontaneous source/rank pairs embed separately and reuse the shared heightening helper.
- Reused selectSpells, not a new AI stage. New PC task selects exact catalog names and rank enum slugs (locally decoded to numeric ranks) with a deterministic 3072-token ceiling. Strict validation caps picks by module-owned counts, rejects unknown/malformed picks, deduplicates cantrips/repertoire entries, and preserves intentional prepared repetitions. The shared candidate limiter reserves per-rank choices within its existing cap so exact first-draft names cannot starve high-level cantrip/repertoire selection. NPC selection retains its old task/shape.
- Builder independently verifies real spell type, traits, tradition, base rank, ritual/focus exclusions, identity and per-rank caps; stale location state is discarded. Review caught the actual ritual field (`system.ritual`, not a trait), inherited numeric AI output violating the new-path enum contract, and cross-pack bare-ID collision risk. Final identity uses the loaded document UUID, with pack-qualified fallback.
- Base-only coverage is explicit in preview and README. Font/curriculum/feat bonus slots, subclass-granted spells, signature spells, variable patron/bloodline consistency, complete spellbooks/familiars, and unsupported/legacy/complex class profiles remain manual. No bonus slots were guessed or converted into unrestricted slots. Empty known plans keep a native entry for manual completion; no unplanned draft substitution.
- Verification: 40 regression files pass, all `.mjs` syntax checks pass, module/localization JSON and `git diff --check` pass. Tests exercise production AI requests with mocked transport, enum/member/count checks, candidate reservation, prepared IDs across ranks, cantrips, source rejection, cross-pack identity, heightening, empty plans and disabled casting. UI coverage is static wiring only. No real provider or live Foundry calls in this pass; actual rendering/casting/expending and class-choice interactions require VPS QA after authorized publication.
- Independent Sol final review approved this bounded base-profile change after rechecking production code, source schemas and all four targeted regression files. No actionable findings remained; the broader automation goal is still incomplete.

## 2026-08-28 — Grounded character choices and native creation feedback (local branch)

- Continued locally while awaiting publication permission. Terra implemented the choice collector/builder seam; root implemented the provider operation, UI feedback, token-error accounting, and integration tests. Sol independently reviewed the combined diff and approved it after two findings were fixed.
- Replaced arbitrary first-option selection with a single bounded batch over the final cloned item sources before `Actor.create`. Class-key-ability, unambiguous concept matches, and sole options can resolve locally; other supported choices receive group-scoped opaque option IDs. The model never receives write destinations or returns actual rule values. Exact membership and duplicate-answer checks run before original values are written to direct `ChoiceSet.selection` or one-level `GrantItem.preselectChoices`.
- Native fallback is intentional: no callback, failed/malformed response, unsupported choices, optional/ignored/drop-enabled rules, unevaluable predicates, unknown UUID labels, and colliding grant flags remain untouched. Existing authored selections/preselect records are preserved. Fixed request bounds are 24 groups, 32 options per group, and 512 options total; overflow stays native, never a truncated list. The old duplicate helper traversal APIs were consolidated into one collector.
- Added a deterministic 3072-token choice task using the existing provider/auth/JSON/retry path. Labels are localized. The generator records the choice call's tokens, warns on unanswered selections, and distinguishes model selection from native feature application with escaped, accessible status text. It tells the GM to complete any remaining PF2e dialogs without claiming actual dialog detection.
- Review caught two defects before commit: mixed unsupported option shapes could collapse into a fake forced choice, and exhausted JSON retries lost token usage when callers recovered through native input. Whole-catalog rejection and accumulated failure usage now have regression tests (including a nonretryable error after an earlier spent attempt).
- Verification: all 37 regression files pass, including production provider requests, callback-before-actor ordering, preserved HP finalization, strict group IDs, numeric values mapped locally, authored selections, native fallback, request bounds, and UI contracts. Changed-module syntax, JSON parsing, and patch whitespace checks pass. Source behavior was independently checked against [PF2e ChoiceSet](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/rules/rule-element/choice-set/rule-element.ts) and [GrantItem](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/rules/rule-element/grant-item/rule-element.ts), plus master: preselection uses exact source values and skips option-predicate validation, which is why the conservative exclusions matter.
- Not live-tested or published. ABC-generated/deeper/dynamic choices still require native handling; this is not complete unattended character generation. VPS QA must check actual selection/grant behavior, progress transitions, provider-failure fallback, and final HP. Item Forge remains deferred.

## 2026-08-28 — PC creation finalization (local branch)

- On `codex/pc-creation-finalization`, Terra implemented a narrow fix for newly created PCs finishing below full HP. The old 9999 seed was set before native item creation; PF2e's per-item `_preCreate` updates current HP by an individual item's max-HP delta, while the complete embedded batch later derives the final maximum. We now use that system-derived maximum after awaiting embedded creation, with no module HP math or max-HP write. Existing native grant expansion and explicit item IDs remain intact.
- The final current-HP update and existing Intelligence-language expansion share the creation rollback boundary. Invalid derived maxima or a rejected final update fail creation; rollback failures log separately without replacing the original error. This only finalizes the newly created actor, not existing actors.
- Source checks: [PF2e 8.4.1 character preparation](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/actor/character/document.ts) derives and clamps HP; [item document lifecycle](https://raw.githubusercontent.com/foundryvtt/pf2e/pf2e-8.4.1/src/module/item/base/document.ts) awaits native pre-create rules and adjusts current HP per item. The same behavior was checked on master. Some `_onCreate` rule updates are detached (including `LoseHitPoints`), so awaiting embedded creation is not a universal promise that every rule/module follow-up has settled. No timing delays or native-hook overrides were added; unusual HP-mutating rules require live QA.
- Added production-import `pc-builder.finalization.test.mjs`: different pre/post-embed HP and Int, await ordering, native keepId, HP-only finalization with no bonus languages, no max/temp writes, invalid-max rejection, final-save rollback, and original-error preservation if cleanup also fails. All 35 regression files, touched-module syntax, module/localization JSON parsing, and patch whitespace checks passed. These mocked lifecycle tests do not replace VPS Foundry QA.
- Independent Sol review approved the scoped Terra implementation and final tests with no remaining actionable findings; its detached-onCreate caveat is recorded above. No push, PR, merge, or deployment performed. User permission is required before authenticated GitHub writes. Item Forge remains deferred.

## 2026-08-28 — Reconciled Fable/Claude audit, merges, and live QA

This record consolidates the previous HANDOFF sections, which mixed an early "no fixes / QA blocked" snapshot with later completed work. Git/API reads now place `origin/main` at `004bffc` (PR #82) and the latest release at v0.3.5.42. The audit stack merged through PR #77; PRs #79–#82 added cross-app UI consistency, QA documentation, partial ChoiceSet automation, and PC named-loot budget/cross-bucket dedup. These are existing work, not new fixes by this session.

- The previous adversarial audit identified: wrong PC wealth table; True-grade rune keys; preset-delete XSS; swallowed SSE provider errors; unvalidated NPC IWR/sense/language slugs; uncapped spell ranks; heritage/ancestry mismatch; condition-activation text and macro-name escaping; PF2e-overwritten rune price writes; party-size reset; unenforced spellcasting toggle/progress overflow; missing heightened spell location; release-path JSON validation; and equipment cost omitted from PC wealth. The code fixes were integrated; the release-workflow `lang/en.json` validation gap remains open. Prior notes also mention 18 lower-severity findings without retaining their full list here—do not invent them or assume they were fixed. The feat-slot allowlist finding was refuted.
- Prior live native-grant QA on v0.3.5.34 (Foundry 14.365 / PF2e 8.4.1) embedded Dwarf / Rock Dwarf / Warrior / Fighter. Clan Dagger feat and weapon, Reactive Strike feat and action, Shield Block, and background Intimidating Glare appeared with derived stats. That agent deleted its temporary native-only actor afterward. Native `PickAThingPrompt` dialogs must be answered while the embed promise waits.
- Fable's v0.3.5.39 QA checked True/Lesser rune keys, heightened-level helper, PC wealth levels 1/5/20, heritage validation, escaped hostile preset names, and UI down to 360px. The in-app browser's AI fetches were blocked, but the user's real browser via Claude in Chrome subsequently completed the cloud workflows. Do not carry that earlier browser limitation forward as a universal provider failure.
- Full cloud QA recorded connection usage 442/8/450; level-7 "Stormcaller Shaman" (24/27 grounded matches, HP 86 / AC 24, Gust of Wind grouped at heightened rank 2); and level-5 "Bryndra Ironveil" (31/31 matches, 49 items after native choices, HP 80 / AC 23, equipment-value deduction active). The PC run exposed named-loot overbudget and equipment/loot duplication, subsequently fixed by PR #82; those fixes were not live-retested in this session.
- "Stormwarden's Amulet" (level 8, 450 gp) was created with a cloned Resistance rule and a linked 1/day companion macro. This is one prior passive/activated Item Forge test, not proof of every forge path. Item Forge is now deferred at the user's request.
- Prior QA artifacts "Stormcaller Shaman", "Bryndra Ironveil", and "Stormwarden's Amulet" plus its macro were intentionally left in the test world. This session has not removed them; ask before cleanup.
- Automation audit against PF2e master and tag `pf2e-8.4.1`: existing direct-item `rule.selection` and one-level `GrantItem.preselectChoices` reuse the native mechanism. ABC-generated features, deeper grants, and dynamic/predicated choices can still prompt. Closing a required prompt can mark a rule ignored without rejecting actor creation; "Created" is not proof all choices are complete. Preselected options bypass native option-predicate validation, so preserve the resolver's predicate exclusions. Next small UX improvement: persistent creation guidance about native dialogs; do not claim actual dialog detection or delete an actor merely because a prompt was dismissed.
- Attribution correction: old Clan Dagger warnings captured during an earlier page reload predated the subsequently generated character. They are not evidence that the new actor's native grant chain failed, and do not justify rewriting official Rule Elements.

## 2026-08-27 — Live Foundry QA and follow-up fixes

- Connected a configured cloud OpenAI-compatible provider to the production Foundry V14 / PF2e 8.4.1 test world. The in-app **Test provider connection** path succeeded with the configured model and reported provider token usage.
- Exercised the full creature preview, non-spellcasting character, and encounter paths. The character preview matched 23/24 grounded picks; its created level-1 sheet had derived HP, AC, saves, ancestry, heritage, background, class, feats, and inventory. The encounter created a 30/40 XP trivial encounter folder and actor. Test actors remain in the world intentionally.
- Live QA found and fixed six release blockers in PR #70: closing a successful **Save & Test** dialog no longer dereferences a destroyed application element; generation modes now have their own legend, visible preview, primary action state, and retained prompt draft; the PF2e 8 ability glossary's current pack id is accepted alongside older ids; Compendium Sources no longer calls module-wide behavior Item Forge-only; and PCs now embed items through PF2e's native document pipeline so recursive grants and Rule Elements execute. The observed prior failure was a Dwarf's missing Clan Dagger with an unresolved `clanWeapon` validation warning.
- Validation for PR #70: 50 JavaScript syntax checks, 26 regression files, JSON validation, `git diff --check`, and GitHub PR workflow run #20 all passed. The fix is deliberately not claimed live-proven until it is merged, the test world updates, and the character-grant path is re-run.
- Remaining live gap: Item Forge is still hidden from ordinary UI until one actual item is forged and inspected. It can be opened with the documented GM-only API, but creating the temporary Foundry macro required for that test awaits user authorization.

## 2026-08-26 — Consumer readiness pass (cloud/local providers and compact UI)

- Modernized first-party OpenAI Chat Completions requests to use a `developer` message and `max_completion_tokens`; other OpenAI-compatible providers retain the broadly supported `system`/`max_tokens` shape. A targeted 400/422 compatibility retry can remove rejected optional controls or negotiate the token-limit field in either direction without looping.
- Made provider failures compact and actionable across model discovery, connection tests, and generation. Error bodies are converted from JSON/HTML to bounded plain text; 401/403, 404, 429, other 4xx, and 5xx responses receive targeted recovery guidance; and a 200 response that is not Chat Completions JSON now reports an endpoint/compatibility problem instead of leaking a parser exception.
- Added a read-only pull-request workflow so syntax, regression, JSON, and patch-format checks run before merge. Previously GitHub reported no checks on PRs at all; validation happened only after `main` had already changed or a release was triggered, even though merges to `main` initiate the public release pipeline.
- Added explicit empty-model and HTTPS-to-HTTP mixed-content checks before generation. Provider identity is inferred from the exact request URL, with public hosts on Ollama/LM Studio ports no longer treated as local or keyless.
- Added an always-visible, compact provider/model strip to the generator and item forge. Responsive rows now wrap cleanly into two columns in narrow Foundry windows; browser layout checks at 420 px and 760 px found no horizontal overflow after the fix.
- Added accessible names to every compact icon-only action in the generator, item forge, and preset manager; associated visible form labels with their fields; and exposed provider readiness as a named status rather than relying on color/icon tooltips. A static template contract prevents unnamed icon buttons or detached labels from returning without increasing their visual footprint.
- Restored native keyboard and assistive-technology access to the Single/Encounter/Character mode tiles. The radios were hidden with `display: none`, removing them from the accessibility tree; they are now visually hidden but focusable inside a named radio group, with a clear focus ring on the compact tile.
- Added semantic feedback to the compact async UI: the shared visual bar now exposes its 0–100 progress value, indeterminate work uses a polite status, provider and item-availability warnings are statuses, and generation failures are alerts. The rapidly changing token detail stays outside a live region so screen readers are not interrupted on every streamed chunk.
- Preserved unsaved generator and item-forge form values before the legacy-key **Authorize API Key** action refreshes the provider strip. Configure already captured the form, but direct authorization did not, so an upgraded GM could type a concept, authorize an existing key, and have the re-render erase that draft.
- Locked the complete provider form during Save & Authorize, Load Models, and Save & Test. Endpoint/model/key fields and competing actions can no longer change while an asynchronous save or provider request is in flight, preventing the dialog from testing one configuration while displaying another or erasing a late edit when model discovery re-renders.
- Made the production SSE reader consume a provider's final `data:` record even when the stream closes without a trailing newline. Small custom/cloud gateways can legally end at EOF; leaving that record buffered previously turned a complete response into a false empty-response retry and could lose final token usage.
- Added a focused first-run provider setup reachable from module settings or the generator strip: cloud/local presets, exact model and endpoint fields, browser-only key handling, endpoint authorization, local CORS/mixed-content guidance, and a 64-token connection check through the real streaming generation path. Switching endpoints without entering a replacement key clears the old secret rather than leaving it available for accidental reuse.
- Removed the client API key from Foundry's ordinary settings form because String settings render as visible text. Existing client-scoped values remain intact, but keys are now editable only through the masked provider setup and its explicit endpoint-authorization flow; a registration-level regression assertion prevents the plaintext field from returning.
- Ran the production `testProviderConnection()` in a real browser page against a separate-origin OpenAI-compatible SSE server. Verified browser preflight/CORS, a named `reasoning_effort` rejection followed by the compatibility retry, streamed JSON assembly, and exact usage. Repeated with an exactly bound bearer key (Authorization arrived) and an unbound stored key (Authorization was withheld). This closes the browser-transport/auth mechanics gap, but is not a substitute for a real provider or Foundry ApplicationV2 test.
- Ran the production provider functions against a real local Ollama service with `qwen3.8:latest`: `/v1/models` returned the installed model through both Node and a separate browser origin (real CORS), and the 64-token Chat Completions check completed with exact provider usage (36 prompt, 6 completion, 42 total). A first attempt correctly hit the 90-second inactivity timeout while another ~97k-token job saturated the single model slot; the warm idle retry passed in 26.8 seconds. Timeout guidance now calls out local model loading/contention instead of implying every silence is a bad endpoint.
- Followed the connection probe with a real direct `generateConcept()` request against the same idle Ollama service. It streamed and parsed a complete 26-field level-1 creature concept in 10.7 seconds, with exact provider usage of 2,736 prompt, 1,478 completion, and 4,214 total tokens. This verifies the local production concept path, but not the Foundry UI configuration flow or actor creation.
- Exercised the other two local AI entry points directly as well: `generatePCConcept()` returned a complete 27-field non-caster level-1 character concept in 11.9 seconds (2,807 total provider tokens), and `generateMagicItemConcept()` returned a 10-field level-3 common item concept in 2.0 seconds (1,528 total tokens). Both satisfied their request-level invariant checks. Compendium grounding, ApplicationV2 rendering, and Foundry document creation remain unverified.
- Updated the stale OpenAI model example to `gpt-5.6-luna`, which the current official model catalog lists for Chat Completions, streaming, and structured output. Added production-import provider/request tests and a static template/CSS contract check.
- Rechecked every guided preset against current first-party documentation: DeepSeek accepts both documented API-root forms and lists `deepseek-v4-flash`; OpenAI lists `gpt-5.6-luna` for Chat Completions; OpenRouter documents `/api/v1`; and Ollama/LM Studio document the preset localhost ports and `/v1` compatibility routes. Setup guidance now gives the exact Ollama `OLLAMA_ORIGINS` and LM Studio CORS controls instead of only generic browser advice.
- Integrated the reviewed item-forge armor-rune gate from `claude/todo-implementation-tv2sw8`. Base candidates now retain their real `system.category`; category-restricted property runes are annotated for the AI and rechecked during normalization, so an illegal pick is dropped without consuming a potency slot. Material-constrained runes remain excluded because the available index data cannot prove the base material. The boundary test verifies both the pure gate and the real compendium-index field flow.
- Made provider URL construction tolerant of a pasted full `/chat/completions` endpoint and gateway query parameters. Request paths are now assembled with the URL API instead of string concatenation, preventing doubled routes and ensuring query parameters remain after the path. The stored URL remains the exact API-key binding identity.
- Added **Save & Test** directly to provider setup. It saves and authorizes first, runs the existing 64-token production-path check, closes only on success, and keeps the dialog open after endpoint/auth/model/CORS failure so the GM can correct it in place. The ordinary save action remains for configuring an offline provider. Action rows now wrap for narrow windows and long translations; real-browser inspection showed all three actions on one row at 520 px and a clean 2+1 wrap at 360 px with no horizontal overflow.
- Closed a permission mismatch between the GM-only Actors-directory button and the public module API. `api.open()` and the hidden `api.openItemForge()` now enforce both GM status and the PF2e system before constructing an app; player console calls receive a clear warning instead of opening provider/generation UI. A production-import regression check covers player denial, GM access, item-forge access, and wrong-system failure.
- Added authorized model discovery to provider setup. **Load Models** saves and endpoint-binds the displayed configuration first, then calls the standard OpenAI-compatible `/models` route with the bound key. Results are validated, deduped, sorted, and exposed as editable datalist suggestions; manual IDs remain supported when a gateway lacks model listing. Suggestions are bound to the normalized source endpoint and clear immediately after a preset, manual URL, or external setting change, so IDs from one provider cannot linger under another. URL tests cover roots, pasted full Chat Completions endpoints, and query-bearing gateways. Real-browser inspection at 360 px found the input/button row and existing footer free of horizontal overflow.

**Still requires live validation before release:** save/test through the new setup in Foundry, one real keyed OpenAI request, the local-provider setup and full actor flow in Foundry, the new strip inside Foundry's ApplicationV2 chrome, and the previously unverified NPC/PC/item-forge surfaces below. Browser CORS, SSE, compatibility retry, bound/unbound key transport, and the direct Ollama concept path are integration-verified outside Foundry.

## 2026-08-19 — Production-hardening pass 1 (provider safety, context bounds, release gates)

- Bound each browser's API key to the exact normalized API Base URL. New, changed, and legacy keys stay disabled until the displayed endpoint is verified and **Authorize for this endpoint** is clicked; changing endpoint invalidates the binding. Keyless Ollama/LM Studio and common local/LAN endpoints no longer show a false missing-key warning.
- Added hard, balanced candidate ceilings: spells 96 total/12 per rank, equipment and loot 120, feats 16 per slot. Removed the narrow-filter path that could expand into an entire compendium. High-level feat prompts now send each repeated feat name once and reference it by short per-slot IDs.
- Added per-operation output/sampling profiles. Grounding calls use temperature 0 and small output limits; creative concept calls retain larger configurable budgets. Separate model reasoning is disabled for structured JSON work, with provider-specific DeepSeek vs. Ollama/LM Studio controls and safe fallback. Temperature 0 now works. Length-truncated and structurally incomplete JSON fails closed instead of being silently repaired into a partial actor.
- Replaced the retired default DeepSeek alias with `deepseek-v4-flash`. Persisted legacy aliases are translated only for DeepSeek's first-party hostname at request time; custom gateway model IDs remain untouched.
- Added production-import regression checks for provider auth, candidate caps, task profiles, compact feat IDs, and JSON parsing.
- Added mandatory syntax/regression gates to every supported release route. Archives exclude `*.test.mjs`; archive validation completes before a GitHub release is created. Directly publishing a GitHub release is no longer a supported trigger because an already-public release cannot be pre-gated.

**Still requires live validation before release:** Foundry v13/v14 settings callbacks, browser calls to keyless Ollama and LM Studio, one remote keyed request after endpoint authorization, and full NPC/PC generation at low and high levels.

## 2026-07-17 — Focus-spell support (PC + NPC), doc restructuring

Roadmap item, previously listed unbuilt. Planned before coding rather than delegated blind:

- **Planning**: an Opus agent (`subagent_type: Plan`) did the real-source research and produced a two-part plan, correcting an assumption that would otherwise have shipped wrong — PC focus points can't be derived by just embedding real class items, because the actual grant mechanism (a Cleric's deity, a Sorcerer's bloodline, ...) is a `ChoiceSet`/`GrantItem` subclass-resolution chain this module doesn't have and isn't worth building for this feature. The plan's workaround: the AI names 1-3 focus spells directly (mirroring how every other real-content pick in this module works), grounded against the compendium by the `"focus"` trait (verified: real focus spells carry an EMPTY `system.traits.traditions`, so the existing tradition-filtered `getSpellCandidates()` can never match one).
- **Part A (PC)**, branch `feat/pc-focus-spells`, commit `03381c3`, built by fable: `ai.mjs` gains an optional `focusSpells` schema field (sibling to `spellcasting`, not nested — a Champion has focus spells with no slots at all); `pc-builder.mjs` clamps/caps at 3 and builds a `prepared:"focus"` spellcasting entry with no `slots` object; `rule-templates.mjs` gains a `focusPool` exemplar kind (clones a real `ActiveEffectLike` rule, e.g. Clarity of Focus's `{mode:"add", path:"system.resources.focus.max", value:1}` — never hand-authored, same safety principle as the item forge) because `character/document.ts` silently zeroes `resources.focus.max` every data-prep pass unless a real Rule Element rebuilds it; `builder.mjs` gains a shared `resolveFocusSpells()` used by both pipelines. `focusPool` is deliberately excluded from `EFFECT_KINDS` (the item-forge-facing kind list) via a new internal `ALL_KINDS`/`EFFECT_KINDS` split, so it doesn't leak into the wondrous-item AI schema as an offerable effect.
- **Part B (NPC)**, branch `feat/npc-focus-spells` (stacked on Part A), commit `3755715`, built by fable: same entry shape, but NPCs need NO Rule Element at all — `npc/document.ts` merges `system.resources.focus` with the actor's own source data winning, so `{value, max}` set directly on the actor's system data just persists. Gated on `concept.spellcasting` being present (a focus-only NPC with no casting tradition has no DC to cast from — explicitly out of scope for v1, not built), enforced twice (normalize-time force-to-`[]`, and again at resolve-time since `#refineSpells` can null `concept.spellcasting` after normalize already ran).
- **User sign-off on 3 judgment calls** (pool size, focus-only-NPC scope, AI-opt-in-vs-hardcoded-rule) — all three took the plan's recommended default. Reasoning preserved in code comments at each block (`builder.mjs`'s NPC focus block cites the same "defensible default, not GM-Core-verified" caveat already used for `TREASURE_BY_LEVEL`).
- **Verification**: 2 new pure-logic self-checks (`pc-builder.focusSpells.test.mjs`, `builder.focusSpells.test.mjs`) cover the cap-at-3 clamp and the NPC's spellcasting-gate defense-in-depth — all 7 self-checks (5 prior + these 2) pass, `node --check` clean on every touched file. Everything else (the cloned RE actually producing a nonzero pool max on a real sheet, the entry rendering as castable, `resources.focus` surviving data-prep in practice, whether the AI's focus-spell picks are actually sensible) is unverified without a live Foundry world.
- **Not merged.** Both branches sit stacked, unpushed, pending a live-test pass — this is a schema-dependent feature in exactly the shape that's bitten this module before (see the bug log below), so budget for at least one live-test bug round before trusting it.

**Doc restructuring (same day)**: an Opus agent audited `CLAUDE.md` for whether it actually works as onboarding material for a cold-start engineer, not just a session-continuity log. Verdict: partially — technically accurate (nothing stale found on cross-check), but no glossary, "This session" used as a heading 4 times for 4 different actual sessions with no dates, setup/verification info buried at the bottom, and the file had grown to ~30k tokens of mostly historical narrative loaded every session. Split into a lean always-loaded `CLAUDE.md` (intro, glossary, architecture, key files, how-to-work-here, current state, known gaps) and this file (full narrative + bug log), per that audit's recommendation.

## 2026-07-16 — Regression self-checks for #57-#61, GitHub issue closeout

Audited PRs #57/#59/#60/#61's fixes against real pf2e source a second time (pulled `item/abc/document.ts`, `actor/character/feats/collection.ts`, `actor/character/feats/group.ts` live from `raw.githubusercontent.com` — reachable this session, unlike PR #60's 403s) and confirmed the granted-feat-location logic is structurally correct. Since no live Foundry world was available, added 5 standalone regression self-checks (zero deps, `node:assert/strict`, `node scripts/<name>.test.mjs`) covering the deterministic logic behind the 5 highest-value live-check items from the prior session's handoff — each ports the real logic verbatim (with source-line citations) where the real function touches Foundry globals, or imports the real function directly where it's pure:

- `scripts/pc-builder.grants.test.mjs` — background-granted feat resolves `system.location` to the granting item's embedded id and lands in the real hidden "skill" slot, not Bonus Feats.
- `scripts/pc-builder.wealth.test.mjs` — real `pcStartingWealthGp()` import: level-1 flat 15gp (not the table's 175gp) across all three treasure-amount settings, table path intact above level 1.
- `scripts/compendium.rarity.test.mjs` — the `RARITY_RANK` cap comparison excludes rarer entries correctly at each cap, including the `??`-fallback and missing-rarity-field edge cases.
- `scripts/ai.lootGuide.test.mjs` — `lootGuide()`'s `subject="character"` framing (purchases tied to backstory) vs. the NPC default (drops tied to description).
- `scripts/generator-app.matchSummary.test.mjs` — the "X/Y compendium matches" badge's found/total aggregation, including the null-on-empty guard and falsy-entry handling.

**Coverage limits, stated plainly**: these check the logic that FEEDS the live behavior, not the live behavior itself — none of them can confirm the AI's actual item picks (wealth-buys-magic-items) or that the checkmarks/badge actually render correctly in the DOM (`templates/generator.hbs` + `styles/simplypf2e.css`, still genuinely UI-only and unverified). Issues **#50**, **#52**, **#53**, **#56** were closed as resolved at the user's explicit direction despite this gap — if live play surfaces something still broken, reopen the relevant issue rather than assuming the closure means it was live-confirmed. **#50 item 8** (TREASURE_BY_LEVEL primary-source check) remains genuinely unverified against a book/Archives of Nethys source — closed along with the rest of #50, but nothing new closed that particular gap.

Committed and pushed directly to `main` (not branch+PR) at the user's explicit one-off direction — README/CLAUDE.md updated same commit.

## 2026-07-15 — PC generator: initial build, first bug-fix pass, round 2 (three separate sessions, same calendar day)

### Early: PR #49 — Player Character generator mode ships

Repo got renamed/migrated from `simplyjaytea/pf2eainpcgen` to `simplyjaytea/simplyPF2e` mid-session. Built and merged **PR #49**: an encounter-mode random-theme dice button, and a new **Player Character generator mode** (new files `scripts/pc-builder.mjs`, `scripts/pc-tables.mjs`; touched `ai.mjs`, `compendium.mjs`, `builder.mjs`, `generator-app.mjs`, `sources-app.mjs`, `generator.hbs`, `lang/en.json`).

Core design insight: PCs don't need a benchmark-table math layer like NPCs do — once real Ancestry/Background/Class/feat items are embedded on a `type: "character"` actor with the right `system.build` data, the PF2e system's own derived-data prep computes AC/HP/saves/proficiencies itself. Ancestry/Background/Class come from a free-text prompt (AI drafts names, a grounded pass matches them against real `pf2e.ancestries`/`pf2e.backgrounds`/`pf2e.classes`/`pf2e.heritages` packs — heritages needed their own 4th pack category, they don't live inside ancestries). Feat slots filled from `pf2e.feats-srd`, discriminated by the feat's real `system.category` field. Spells/equipment/loot reuse `#refineSpells`/`#refineEquipment`/`#refineLoot` and `resolveLoot`/`applyTreasureBudget` completely unchanged by normalizing the PC concept to carry the same fields those helpers already read. Starting wealth reads `T.TREASURE_BY_LEVEL.total` scaled by `TREASURE_AMOUNT_MULTIPLIER`, deliberately NOT `treasureBudget()` (wrong divisor — that one's for an NPC's per-encounter share). Single-class only, fully automatic, both explicit v1 scope decisions.

**Shipped with a real, acknowledged risk**: no running Foundry+pf2e world was available while building this, so the exact `system` field names `createCharacterActor()` writes were asserted from training knowledge, not verified against live data — marked with a `SCHEMA NOTE — ASSERTED, NOT VERIFIED` comment with exact verification steps, called out as PR #49's first test-plan checkbox. It merged anyway (see bug log below for what that cost).

Process note: implementation was delegated to a `fable`-model agent in an isolated worktree after codebase research + an approved plan were done directly; the agent independently found real refinements (the heritages-pack split, the `system.category` feat-filter field) not in the original plan.

### Mid: PR #54 — first live-test bug-fix pass

User tested PR #49 live and filed GitHub issues #50-#53. Root-caused #50 items 1-7 and #51 by pulling real `foundryvtt/pf2e` source instead of guessing again:

- **Attribute boosts**: the actor's `system.build.attributes.boosts` only holds the tier-level free boosts (1/5/10/15/20) — ancestry's 2 free boosts, background's 2 boosts, and the class's key-ability boost are NOT actor-level data at all. Each ABC item has its own `system.boosts[*].selected` (ancestry/background) or `system.keyAbility.selected` (class), and that item's own `prepareActorData()` pushes the selection into `actor.system.build.attributes.boosts.{ancestry,background,class}` during data prep. New `assignItemBoosts()`/`resolveKeyAbility()` fix it. Also fixed `build.attributes.manual` from `true` to `false` (manual mode skips boost application entirely).
- **Feats in Bonus feats instead of their real slot**: `feats/group.ts`'s `assignFeat()` requires `system.location` to equal the slot id (`"<group>-<level>"`, e.g. `"ancestry-1"`), not the bare group name. Fixed by setting `system.location` + `system.level.taken` from the resolved feat slot.
- **No spell slots**: `spellcastingEntry.system.slots` was never populated. New `spontaneousSpellSlots(level)` in `pc-tables.mjs`, spot-checked against the known Player Core table.
- **0 starting HP**: `system.attributes.hp.value` never set. `character/document.ts`'s HP prep does `stat.value = Math.min(stat.value, stat.max)` every pass — seeding `hp.value: 9999` reliably clamps to the real computed max.
- **Missing bio/appearance**: `CharacterBiography` has no `.value` field — real fields are `.backstory`/`.appearance`.
- **Skills not selected**: `class.system.trainedSkills.additional` (a free-pick count) was never filled; background Lore needs a real embedded `type:"lore"` item, not a `system.skills` entry.
- **Missing armor**: prompt-only fix — `pcSystemPrompt()` never mentioned armor at all.
- **#51 — background lookup aborts the whole generator**: `getPacksFor()` hardcoded exactly one pack id per category, missing add-on/Lost-Omens compendiums. New `getAllPacksFor(category)` unions configured + auto-detected packs.

Process note: planned + verified directly (pulled real pf2e source via `gh api`/`curl`), delegated implementation to an Opus subagent with those source files as ground truth, then independently re-verified the highest-risk part (the attribute-boost chain) rather than trusting the subagent's self-report. **Merged without the PR's own test plan being run** (live in-Foundry generation) — user asked to push+merge immediately.

### Later: PRs #57-#61 — round 2, issue #56

User ran the live-Foundry test PR #54 had skipped, and — as flagged at the time — found MORE real bugs: filed **#56** (6 items: height/weight, languages, magic items missing from starting equipment, feats still sometimes not taken, skills stuck at Trained forever, missing personality/bio fields). The SCHEMA NOTE pattern bit a second time even after a "verified against real source" round, because that round only verified the fields it happened to touch — untouched fields were never written at all and nobody had checked. Shipped across 4 PRs, each independently merged same-session at the user's request:

- **PR #57** — #56 items 1/2/3/6: age/gender/height/weight/ethnicity/nationality (`details.*.value`), languages (`details.languages.value`, capped to the ancestry's own `additionalLanguages.count`/allowed-list), magic items nudged into the equipment prompt, personality/likes/dislikes/allies/enemies/organizations mapped onto real `CharacterBiography` fields (`attitude`/`beliefs`/`likes`/`dislikes`/`allies`/`enemies`/`organizations`) — all verified against `character/data.ts`.
- **PR #59** — #56 items 4/5: re-pulled `feats/group.ts`+`feats/collection.ts` and confirmed PR #54's slot-id fix WAS structurally correct — the actual remaining bug was `resolveFeatPicks()` having no fallback when the AI's single batched `selectFeats()` call dropped a slot or hallucinated a name: the slot just stayed permanently empty. Now falls back to the slot's own first candidate; also fixed a missing category filter. Skills: new `assignSkillRanks()` finally wires up `SKILL_INCREASE_LEVELS` (existed since PR #49, never consumed) — round-robins proficiency increases up to the CRB's level gates.
- **PR #60** — #52 (every matched category shows a positive checkmark, new "X/Y compendium matches" header badge) plus a re-check of `TREASURE_BY_LEVEL` for #50 item 8. **Could not reach a primary source**: every external fetch (WebFetch, Archives of Nethys/pf2easy/pf2calc, even a Wikipedia control fetch) returned 403 — a session-wide restriction, confirmed by the control fetch failing identically. Documented honestly instead of claiming a verification that didn't happen. Only `raw.githubusercontent.com` and session-scoped GitHub MCP tools were reliable in that environment.
- **PR #61** — user reported 2 more bugs + 2 feature requests directly in chat (never filed as issues). Background/ancestry/class's own automatically-granted feat (e.g. Acolyte's "Student of the Canon", granted via `system.items`) was landing in Bonus Feats. Root cause: `ABCItemPF2e.createGrantedItems()` sets the granted feat's `system.location` to the GRANTING ITEM'S OWN embedded id, matching a hidden slot the Skill Feats group builds specifically for it — the grant was cloned but `location` was never set. Fixed by pre-assigning explicit `_id`s to the ancestry/heritage/background/class item data and wiring each grant's location to its parent. Also shipped: PC starting wealth drafts+grounds a real magic-item wishlist via new `generatePCLoot()` (`lootGuide()` gained a `subject` param for "purchased" vs "dropped" framing); a Max-rarity select filters ancestry/background/heritage candidates by `system.traits.rarity`; and `pcStartingWealthGp()` was found to give a fresh 1st-level character ~175 gp instead of the Core Rulebook's flat 15 gp.

## 2026-07-14 — UI polish, preset overhaul

PRs #43-48: icon overhaul (consistent FA6 icons), unified UI spacing/sizing/typography, clearer generation progress labels + accurate token counts, Single/Encounter mode icon pairing, action-type-only ability lookups, and the **preset overhaul** (generator defaults captured per-preset, edit-in-place, duplicate, Manage Presets dialog with export/import). No detailed session writeup was recorded for this day at the time — this entry is reconstructed from PR titles/merge times only, not from a contemporaneous narrative.

## 2026-07-12 — Item forge Phase 3, loot/CI fixes, auto-release

- **Magic item forge Phase 3** (branch `feat/itemforge-phase3`, builds on Phase 1/2): rune-based magic weapons/armor. A "kind" select (Wondrous Item / Weapon / Armor) switches to a different, simpler pipeline. **Key discovery**: fundamental AND property runes are just real Item documents in the equipment compendium with their own real level and price — verified via `packs/pf2e/equipment/*.json`, `weapon/data.ts`, `armor/data.ts`. So a runed item needs no empirical price benchmark and no memorized rune list: pick real base weapon/armor + real rune items, SUM their real prices, overall level = MAX level among them. Candidate harvesting filters base items by type/level, property runes by their real `system.usage.value` string, fundamental tiers by resolving each tier's real item level against the target. **The one non-obvious transform**: `system.runes.property` stores rune keys in camelCase ("ghostTouch", "greaterFlaming"), not the kebab-case slug the item's name would normally produce — verified against `WEAPON_PROPERTY_RUNES`/`ARMOR_PROPERTY_RUNES` in `runes.ts`. `kebabToCamel(slugify(name))` bridges the two (see bug log for the graded-rune follow-up bug). Known gaps: no rune prerequisite/exclusivity checking, armor-category filtering is loose, shield/ammo runes out of scope.
- **Loot over-generation fix** (issue #28, branch `fix/loot-budget-and-scaling`): two independent causes. (1) Encounter mode gave EVERY role group the FULL per-encounter `treasureBudget()` instead of one shared budget split across groups — fixed by dividing by `members.length`. (2) The AI never saw the Stingy/Standard/Generous amount setting at all — `LOOT_GUIDE` became `lootGuide(amount)`, `SYSTEM_PROMPT` became `systemPrompt(amount)`, with a `LOOT_AMOUNT_GUIDE` map nudging item count/richness.
- **Auto-release CI** (branches `ci/auto-release-on-merge` → `fix/auto-release-workflow-call` → `fix/release-tag-detection`, 3 PRs to get right): every push to `main` auto-ships a release, bumping the last dot-segment of the latest tag. Two real bugs found only by merging and watching it run live — see bug log.
- **Item forge button discoverability** (issue #27): added a second, more visible entry point next to the Single/Encounter mode toggle in the generator window itself, calling the same `openItemForge()` singleton the sidebar button opens.
- **Treasure overpay fix** (branch `fix/treasure-per-level-overpay`): `ENCOUNTERS_PER_LEVEL` had been hardcoded to 4 as a guess; live play showed loot running ~3x too rich. Fixed to the GM-Core-derived `1000/80 = 12.5` (1,000 XP to level ÷ 80 XP per Moderate encounter for 4 players).

Process note: had an independent `fable` agent audit the freshly-built item forge Phase 3 diff before considering it done — it found 2 real bugs (see bug log) that manual review + `node --check` had missed. But the audited PR had already been merged by the user moments before the audit finished, so fixes landed in a follow-up PR instead of the original — worth merging AFTER a requested audit completes, not before.

## 2026-07-11 — Treasure budget, item forge Phases 1-2, token-opt, prompt bug fixes

- **Spell selection**: 2-stage keyword-focus-then-pick (cuts unbounded spell-list token dump). **Equipment**: added a `value` field (gp estimate) end-to-end; unmatched equipment falls back to `customEquipmentItem()` instead of vanishing; prompt broadened to explicit adventuring-gear category, count 2-6→3-8. **AI image/portrait generation removed entirely** — bestiary-art matching (`findBestiaryArt`) is now the only art path. **Equipment grounding** (branch `feat/grounded-equipment-matching`): 1-stage pick-from-real-candidates, no AI focus call — keywords tokenized locally, `getEquipmentCandidates(level, keywords)` excludes treasure and caps item level at creature level.
- **Treasure budget** (branch `feat/treasure-budget`): module computes an expected gp value for loot, same authority split as AC/HP (tables own the numbers, AI only themes within them). `TREASURE_BY_LEVEL` (GM Core Table 10-9, per character level) ÷ `ENCOUNTERS_PER_LEVEL` (hardcoded to 4 at this point — see the 07-12 correction above) × `RARITY_TREASURE_MULTIPLIER` × `TREASURE_AMOUNT_MULTIPLIER` = `treasureBudget(level, rarity, amount)`. Design: encounter mode looks up PARTY level, not each member's own; each role group gets one constant treasure share split evenly across its current copies. Enforcement (`applyTreasureBudget`) flexes ONLY coin entries (±20% tolerance), never deletes named items. CAVEAT: table rows 13-20 were recalled with lower confidence than 1-12; -1/0/21-24 rows are extrapolated, not published — re-check against the book if high-level treasure feels off.
- **Magic item forge Phase 1** (branch `feat/magic-item-forge`): describe a wondrous item → real Item document with working passive Rule Elements. Safety principle established here and reused everywhere since: **clone real REs, never hand-author them** — `rule-templates.mjs` scans installed Item packs at runtime for published items already carrying each RE kind, exact-shape whitelists the rule, and `buildMagicItemData()` clones+parameterizes only value/selector/damage-type. 6 effect kinds shipped with runtime-found exemplars (itemBonus, resistance, weakness, immunity, sense, speed). Kinds with no exemplar in a given world are dropped from the AI's schema dynamically, never hand-authored as fallback. Pricing is empirical: median real compendium gp at the item level, window widens when `<5` samples.
- **Magic item forge Phase 2** (branch `feat/magic-item-forge-phase2`): 1/day ACTIVATED item effects via a click-to-run companion macro, not passive rules. **The AI never writes code** — it supplies only numbers/enum slugs, embedded as `JSON.stringify` constants atop a pre-written script body (zero injection surface). Four templates: damage, heal, condition, selfBuff. Every non-trivial API call is try/catch → plain-chat-message fallback. Several APIs (`actor.saves[slug].roll()`, DamageRoll-triggers-Apply-buttons behavior, `actor.increaseCondition()`, the `effect` item's duration shape) could NOT be verified against real system source on the machine available at the time — recalled-and-wrapped, not verified against ground truth; flagged as the first things to check if a macro misbehaves. Charges tracked via a module flag (`flags.simplypf2e.forge.uses`) rather than the system's native fields, since those couldn't be verified either.

Process note: mid-session, `fable` hit its monthly spend limit and failed a task outright (no code, no branch) — switched to Opus for Phase 2, which the user wanted done carefully. An independent line-by-line review after Opus's build (not just trusting its self-report) found+fixed one real bug (see bug log). Also found a PR (#24) already created+merged directly on GitHub outside the session, with a placeholder auto-generated title/body — rewrote its description after the fact per the user's request. Worth checking `gh pr list`/`gh pr view` for surprises like this rather than assuming a session's own view of branch state is exhaustive.

- **Remaster item names / inert passives fix** (branch `fix/remaster-item-names`, commit `8b29a94`): see bug log for both.

## Bug log

Chronological, oldest first. The **Pattern:** lines are the actually-reusable lessons — read these before repeating an investigation.

- **PC generator shipped with 7+ schema field-name/behavior bugs from an unverified assertion** (fixed, PR #54) — the PC generator (PR #49) had a `SCHEMA NOTE — ASSERTED, NOT VERIFIED` comment acknowledging its `system.*` field names were recalled from training knowledge, not checked against a live Foundry+pf2e world. It merged anyway (auto-release ships every main merge). User tested live and every guessed field either had the wrong name or was missing a required companion field (see the PR #54 write-up above for the full list). **Pattern**: "no live Foundry world available" is not a reason to ship a guessed schema — the real system source is public on GitHub and can be pulled and read directly; do this BEFORE writing schema-dependent code, not after a user reports it's broken. A hedge comment ("asserted, not verified") is not a substitute for verification, it's just an honest label on a bug that hasn't been found yet.
- **PC generator's "verified against real source" fix round only verified the fields it touched — untouched fields were still guessed-and-wrong or simply never written** (fixed across PRs #57/#59/#61) — PR #54 fixed the 7 bugs the user had actually found and reported. A second live test (issue #56) found 6 MORE gaps in fields PR #54 never touched at all, and a THIRD pass (PR #61) found a background/ancestry/class's own automatically-granted feat landing in Bonus Feats for a different reason than the AI-picked-feat bug PR #54 fixed. **Pattern**: "verified against real source" is a claim about the fields that were checked, not the whole feature — a schema this large has enough surface area that fixing reported bugs doesn't mean unreported ones don't exist. When a feature has shipped with schema guesses even once, budget for at least one more live-test round finding fields nobody thought to check yet.
- **`ABCItemPF2e`'s own granted feats need a DIFFERENT `system.location` value than AI-picked feats** (fixed, PR #61) — PR #54's fix (`system.location = "<group>-<level>"`) is for feats filling a level-gated SLOT and was verified via `feats/group.ts`. But a background's own built-in feat (granted via `system.items`) needs `system.location` set to the GRANTING ITEM'S OWN embedded `_id` instead, per `ABCItemPF2e.createGrantedItems()`, matched against a hidden slot the Skill Feats group builds specifically for it. Two structurally different feat-placement mechanisms writing to the same field. **Pattern**: when a real system has more than one path that writes to the same field, verifying ONE path's mechanism doesn't mean a DIFFERENT path works the same way — pull the source for each mechanism separately rather than assuming they share a rule because they share a field name.
- **PC starting wealth used the wrong table row for level 1** (fixed, PR #61) — `pcStartingWealthGp()` fed every level, including 1, through `TREASURE_BY_LEVEL`, giving a fresh 1st-level character ~175 gp instead of the Core Rulebook's flat 15 gp. Caught only because a NEW feature (wealth-buys-items) made the bug's effect much more visible. **Pattern**: a benchmark-reuse decision ("this table already has the number I need") should be checked for documented EXCEPTIONS to the table's own applicability, not just for whether the number itself is right.
- **Attack of Opportunity not generating** (fixed, unreleased). Schema comment cited it as a valid glossary-ability example, but the design-guidance enumeration bullet never listed it, so the model almost never picked it. **Pattern**: check whether X is only a schema-comment example vs. actually reinforced in the "Design guidance" section — if schema-only, add it to the relevant enumeration/road-map bullet there too.
- **Gold coins appearing in equipment** (fixed, unreleased). The `equipment` schema comment never told the model not to put coins there, and equipment has no coin-to-currency conversion. Fixed with both a prompt note AND a defensive filter reusing `parseCoins()` to drop any equipment entry that parses as pure coins. **Pattern**: prompt-only fixes are necessary but not sufficient for "wrong bucket" bugs — pair with a cheap defensive filter using logic that already exists elsewhere, so a prompt regression doesn't silently reopen the bug.
- **Pre-Remaster item names leaking through** (fixed, `fix/remaster-item-names`, commit `8b29a94`). `LOOT_GUIDE` already reminded the model to use Remaster names; the `equipment` schema comment never had that reminder at all. Fixed via a shared `REMASTER_NOTE` const interpolated into both. **Pattern**: whenever the model names published content in more than one schema field, grep for where the naming-convention reminder already exists and make sure every sibling field gets it too — ideally via a shared const, not copy-pasted text.
- **Passives were inert prose instead of "just working"** (same commit). Custom (non-glossary-matched) passive abilities only get automation from `enrichDescription()`'s phrasing conventions — anything else is just prose a GM has to remember to apply. Fix (prompt-only, deliberately not a new RE-generation feature): nudged the model to prefer a real glossary match for passives, and to phrase necessarily-custom passives using the existing description conventions.
- **Encounter mode created one actor per GROUP, ignoring `member.count`** (fixed, `fix/encounter-actor-count-and-loot-scaling`). `composeEncounter()` correctly returns groups with counts, but `#createEncounterActors()` called `createActor()` once per member regardless. **Pattern**: when a data structure carries a count/quantity field, grep every consumer to confirm it actually multiplies by it.
- **Loot never scaled up for "hoard"/"lots of loot" requests** (fixed, same branch). Three independent caps each silently flattened abundance: a hardcoded "3-8 items" prompt baseline with no scaling instruction, `generateLoot()`'s context dropping `concept.description` (where hoard language actually lived), and `normalizeLoot()` hard-truncating to 12 entries. **Pattern**: a "model won't do X" bug can have BOTH a prompt cause and a post-processing cause — trace the output through every normalize/clamp step after fixing the prompt, and check the model can even SEE the signal (a token-trimmed context may have dropped the field the instruction keys on).
- **Treasure group budget multiplied by headcount instead of splitting across it** (caught pre-merge, `feat/treasure-budget`). First implementation set the per-copy share to the group's FULL target and multiplied by count for the readout — a 4-copy group got 4x intended treasure. Fixed to `treasureGroupBudget ÷ count`. **Pattern**: a per-group total and a per-copy share are easy to conflate; when a UI stepper can change a count live, check whether the derived value needs to be constant (recompute the split) or already-final.
- **Condition-activation macro would silently auto-apply on an unreadable save result** (fixed, `feat/magic-item-forge-phase2`, found by independent review). If the guessed save-result field shape was wrong, the "did it apply" check defaulted to `true` — silently turning a save-negates condition into an unconditional hit, no error, no warning. Fixed to default to the SAFER outcome (skip + manual-adjudication message) when the result can't be read. **Pattern**: when wrapping an uncertain external API in a boolean gate, make sure the "I couldn't read the result" branch defaults to the safer/weaker outcome — silent over-application is worse than silent under-application, especially for anything with real game-balance stakes.
- **Graded property runes ("Flaming (Greater)") keyed wrong in `system.runes.property`** (found by an independent `fable` audit, fixed after the original PR had already merged). `kebabToCamel(slugify(name))` turned "Flaming (Greater)" into `"flamingGreater"`, but the real key is `"greaterFlaming"` — the grade moves to a LEADING prefix. Would have silently failed to apply the rune's effect — no error, just inert. Fixed by special-casing the `"X (Greater|Major)"` shape and reordering before camelCasing, verified against 4 independent real examples. **Pattern**: when a transform is derived from a FEW verified samples, actively hunt for a shape that breaks the naive version before shipping.
- **Fundamental runes leaking into the property-rune candidate list** (same audit, same fix commit). Weapon/Armor Potency and Striking/Resilient share the exact same `system.usage.value` filter string as actual property runes, so they could be offered to and picked by the AI as a property rune, double-pricing the item. Fixed by explicitly excluding the fundamental catalog names. **Pattern**: when two categories of real compendium item share a filter field, don't assume the filter alone disambiguates them.
- **Auto-release: pushed tag never triggered the release build** (found by testing live immediately after merging). GitHub suppresses `on: push` triggers for refs pushed using the DEFAULT `GITHUB_TOKEN`, to prevent workflows infinitely re-triggering each other — undocumented in the trigger's own syntax. Symptom: tag existed, no error, no release, fully silent. Fixed via `workflow_call` (direct reusable-workflow invocation) instead of relying on a push-triggered chain. **Pattern**: any time one workflow is meant to trigger another via a git push/API action performed BY that first workflow, check GitHub's `GITHUB_TOKEN` trigger-suppression rules first.
- **Auto-release: `github.event_name` inside a `workflow_call` is the CALLER's event, not `"workflow_call"`** (found immediately after fixing the bug above). The fix shipped a real GitHub release tagged literally `"main"`, marked Latest, briefly serving a broken `module.json` on the public manifest URL. Root cause: the tag-detection step checked `event_name == "workflow_call"`, which inside a called reusable workflow reflects the TOP-level run's event instead. Fixed by branching on whether `inputs.version` was actually supplied, instead of inspecting `event_name` at all. Required user sign-off to delete the broken release + colliding git tag. **Pattern**: `github.event_name` and similar top-level `github.*` context fields do NOT reset per reusable-workflow call — a reusable workflow that needs to know "was I called with an explicit input" should check the input itself, never infer it from `event_name`.

## Process notes worth remembering generally

- The user tends to merge PRs themselves right after requesting a review/audit, sometimes before the audit finishes — say so explicitly if a requested review is still in flight rather than assuming it's understood; a merge landing mid-audit needs its fixes shipped as a new follow-up PR.
- CI/workflow (`.yml`) changes specifically need a live test merge to trust — `node --check`/YAML-syntax-validity has never caught a trigger-chain bug in this repo's history (2 for 2 so far).
- An independent second agent auditing a freshly-built diff (not just the builder's own self-report) has found real bugs on at least 3 separate occasions (item forge Phase 2's condition macro, item forge Phase 3's rune-key + fundamental-leak bugs). Worth doing for anything schema-dependent or security/balance-sensitive, even after the primary build already looks clean.
